---
phase: 05-bulk-validation-reporting
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/features/explorer/components/scan-results-tab.tsx
  - src/features/explorer/components/scan-results-header.tsx
  - src/features/explorer/components/scan-file-row.tsx
  - src/features/explorer/components/explorer-shell.tsx
  - src/features/explorer/components/file-content-area.tsx
  - src/features/explorer/components/file-tab.tsx
  - src/features/explorer/store.ts
  - src/features/explorer/utils/scan-csv-export.ts
  - src/features/explorer/utils/scan-csv-export.test.ts
  - src/features/explorer/utils/scan-pdf-export.ts
  - src/features/explorer/utils/scan-json-export.ts
  - src/features/explorer/utils/scan-json-export.test.ts
  - src/features/explorer/utils/scan-clipboard-export.ts
  - src/features/explorer/utils/scan-clipboard-export.test.ts
  - src/features/explorer/hooks/use-scan-export.ts
autonomous: true
requirements:
  - VALD-05
  - VALD-06
must_haves:
  truths:
    - "Scan results open as a dedicated tab in the content area that persists until closed (D-12)"
    - "Results display a summary header with total files, errors, warnings, clean counts (D-13)"
    - "Results show a sortable file list with expandable rows revealing per-file problems (D-13)"
    - "All scanned files appear including clean files with green checkmark; filterable to issues only (D-14)"
    - "Clicking a file in scan results opens it as a regular file tab with Phase 4 behavior (D-15)"
    - "Export dropdown in results header offers CSV, PDF, JSON, clipboard text, and clipboard markdown (D-16)"
    - "CSV export has one row per problem with columns: File Path, Line, Column, Severity, Issue Code, Description, Encoding (D-17)"
    - "PDF export has summary section with scan metadata followed by file-by-file detail (D-18)"
    - "JSON export is structured with metadata and file results array (D-19)"
    - "Clipboard offers both plain text summary and markdown table as separate items (D-20)"
    - "All export formats include metadata: folder path, file pattern, date/time, totals (D-21)"
  artifacts:
    - path: "src/features/explorer/components/scan-results-tab.tsx"
      provides: "Scan results display with sortable file list"
      exports: ["ScanResultsTab"]
    - path: "src/features/explorer/components/scan-results-header.tsx"
      provides: "Summary stats and export dropdown"
      exports: ["ScanResultsHeader"]
    - path: "src/features/explorer/components/scan-file-row.tsx"
      provides: "Expandable file row with problems"
      exports: ["ScanFileRow"]
    - path: "src/features/explorer/utils/scan-csv-export.ts"
      provides: "CSV formatter for scan results"
      exports: ["exportScanToCsv"]
    - path: "src/features/explorer/utils/scan-csv-export.test.ts"
      provides: "CSV export unit tests"
      contains: "exportScanToCsv"
    - path: "src/features/explorer/utils/scan-pdf-export.ts"
      provides: "PDF formatter for scan results"
      exports: ["exportScanToPdf"]
    - path: "src/features/explorer/utils/scan-json-export.ts"
      provides: "JSON formatter for scan results"
      exports: ["exportScanToJson"]
    - path: "src/features/explorer/utils/scan-json-export.test.ts"
      provides: "JSON export unit tests"
      contains: "exportScanToJson"
    - path: "src/features/explorer/utils/scan-clipboard-export.ts"
      provides: "Plain text and markdown formatters"
      exports: ["formatScanAsText", "formatScanAsMarkdown"]
    - path: "src/features/explorer/utils/scan-clipboard-export.test.ts"
      provides: "Clipboard export unit tests"
      contains: "formatScanAsText"
  key_links:
    - from: "src/features/explorer/components/scan-results-tab.tsx"
      to: "src/features/explorer/store.ts"
      via: "scanResult from useExplorerStore"
      pattern: "scanResult"
    - from: "src/features/explorer/components/scan-file-row.tsx"
      to: "src/features/explorer/store.ts"
      via: "openFile action for drill-down"
      pattern: "openFile"
    - from: "src/features/explorer/hooks/use-scan-export.ts"
      to: "src/features/export/services/export-service.ts"
      via: "exportService.saveTextFile and saveBinaryFile"
      pattern: "exportService"
---

<objective>
Deliver the scan results display and report export vertical slice: after a scan completes, the user sees an interactive results tab with sortable file list, expandable problem details, drill-down to individual files, and can export in all five formats (CSV, PDF, JSON, clipboard text, clipboard markdown).

Per VALD-05 (results display) and VALD-06 (export). Implements D-12 through D-21 from CONTEXT.md.

Purpose: Completes the bulk validation story -- users not only scan but also browse results interactively and generate shareable reports.
Output: Scan results tab component, five export formatters with unit tests, export hook, and content area integration.
</objective>

<execution_context>
@/Users/elliotlayen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elliotlayen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/elliotlayen/Developer/Monocle/.planning/PROJECT.md
@/Users/elliotlayen/Developer/Monocle/.planning/ROADMAP.md
@/Users/elliotlayen/Developer/Monocle/.planning/phases/05-bulk-validation-reporting/05-CONTEXT.md
@/Users/elliotlayen/Developer/Monocle/.planning/phases/05-bulk-validation-reporting/05-RESEARCH.md
@/Users/elliotlayen/Developer/Monocle/.planning/phases/05-bulk-validation-reporting/05-PATTERNS.md
@/Users/elliotlayen/Developer/Monocle/.planning/phases/05-bulk-validation-reporting/05-01-SUMMARY.md
</context>

## Phase Goal

**As a** integration analyst, **I want to** scan an entire folder or client's files for problems in one action and export the results, **so that** I can identify and report issues across hundreds of files without opening each one manually.

<tasks>

<task type="auto">
  <name>Task 1: Export formatters with unit tests and scan results components</name>
  <files>
    src/features/explorer/utils/scan-csv-export.ts,
    src/features/explorer/utils/scan-csv-export.test.ts,
    src/features/explorer/utils/scan-json-export.ts,
    src/features/explorer/utils/scan-json-export.test.ts,
    src/features/explorer/utils/scan-clipboard-export.ts,
    src/features/explorer/utils/scan-clipboard-export.test.ts,
    src/features/explorer/utils/scan-pdf-export.ts,
    src/features/explorer/hooks/use-scan-export.ts,
    src/features/explorer/components/scan-file-row.tsx,
    src/features/explorer/components/scan-results-header.tsx,
    src/features/explorer/components/scan-results-tab.tsx
  </files>
  <read_first>
    src/features/export/utils/pdf-export.ts,
    src/features/export/utils/json-export.ts,
    src/features/export/services/export-service.ts,
    src/features/export/hooks/useExport.ts,
    src/features/export/components/export-button.tsx,
    src/features/explorer/components/problem-row.tsx,
    src/features/explorer/components/problems-panel.tsx,
    src/features/explorer/components/file-content-header.tsx,
    src/features/explorer/types.ts,
    src/features/explorer/store.ts
  </read_first>
  <action>
    **CSV export (`scan-csv-export.ts`, per D-17, D-21):** Export `exportScanToCsv(result: ScanSummary): string`. Build a CSV string with header row: `File Path,Line,Column,Severity,Issue Code,Description,Encoding`. For each file in `result.files`: if file has no problems, emit one row with filePath in first column, empty line/column/severity/code/description, and encoding in last column (status "Clean" in Severity column). If file has problems, emit one row per problem with all columns populated. Prepend metadata as CSV comments: `# Scan Report`, `# Folder: ${result.folderPath}`, `# Pattern: ${result.filePattern}`, `# Date: ${new Date().toISOString()}`, `# Total Files: ${result.totalFiles}`, `# Errors: ${result.totalErrors}`, `# Warnings: ${result.totalWarnings}`, `# Clean: ${result.cleanFiles}`. Use proper CSV escaping: wrap fields containing commas, quotes, or newlines in double quotes; escape internal double quotes by doubling them. Export a helper `escapeCsv(value: string): string`.

    **CSV export tests (`scan-csv-export.test.ts`):** Create vitest tests:
    - `exportScanToCsv includes metadata comments`: Verify output starts with "# Scan Report" and contains "# Folder:", "# Pattern:", "# Date:", "# Total Files:".
    - `exportScanToCsv produces one row per problem`: Provide a file with 3 problems, verify 3 data rows for that file (plus header row).
    - `exportScanToCsv includes clean files with Clean severity`: Provide a clean file, verify a row with "Clean" in the Severity column.
    - `escapeCsv handles commas and quotes`: Verify fields with commas are wrapped in quotes, fields with quotes have them doubled.

    **JSON export (`scan-json-export.ts`, per D-19, D-21):** Export `exportScanToJson(result: ScanSummary): string`. Build a structured JSON object: `{ metadata: { folderPath, filePattern, scanDate: new Date().toISOString(), totalFiles, errorFiles, warningFiles, cleanFiles, totalErrors, totalWarnings }, files: result.files }`. Return `JSON.stringify(exportData, null, 2)`. Follow the same pattern as `src/features/export/utils/json-export.ts`.

    **JSON export tests (`scan-json-export.test.ts`):** Create vitest tests:
    - `exportScanToJson includes metadata wrapper`: Parse the output and verify `metadata.folderPath`, `metadata.filePattern`, `metadata.scanDate` exist.
    - `exportScanToJson includes all files in files array`: Provide 3 files, verify parsed `files` array has length 3.
    - `exportScanToJson is valid JSON`: Verify `JSON.parse` does not throw.

    **Clipboard export (`scan-clipboard-export.ts`, per D-20, D-21):** Export two pure functions:
    - `formatScanAsText(result: ScanSummary): string` -- compact plain text block for Slack/Teams/email. Format: "Scan Report - [folderPath]" header line, "Pattern: [filePattern] | Date: [ISO date]" line, blank line, "Summary: [totalFiles] files scanned, [totalErrors] errors, [totalWarnings] warnings, [cleanFiles] clean" line, blank line, then for each file with problems: "  [status icon] [relativePath] - [errorCount] errors, [warningCount] warnings" line. Status icons: "[ERROR]", "[WARN]", "[OK]". Omit clean files from detail list (they are in summary count).
    - `formatScanAsMarkdown(result: ScanSummary): string` -- markdown table for GitHub/Jira. Format: "# Scan Report" heading, metadata as bullet list (`- **Folder:** ...`, `- **Pattern:** ...`, `- **Date:** ...`), "## Summary" heading with stats as bullet list, "## Results" heading with markdown table: `| File | Status | Errors | Warnings | Encoding |` header, separator row, one row per file with relativePath, status badge text, error count, warning count, encoding.

    **Clipboard export tests (`scan-clipboard-export.test.ts`):** Create vitest tests:
    - `formatScanAsText includes header and summary`: Verify output contains "Scan Report -" and "Summary:".
    - `formatScanAsText lists only files with problems`: Provide 2 error files and 1 clean file, verify only 2 detail lines appear.
    - `formatScanAsMarkdown produces valid markdown table`: Verify output contains "| File | Status |" header and separator row "| --- |".
    - `formatScanAsMarkdown includes metadata bullets`: Verify "- **Folder:**" and "- **Pattern:**" appear.

    **PDF export (`scan-pdf-export.ts`, per D-18, D-21):** Export `exportScanToPdf(result: ScanSummary): Promise<Uint8Array>`. Follow the exact jsPDF pattern from `src/features/export/utils/pdf-export.ts`: create `new jsPDF({ orientation: "portrait", unit: "mm", format: "a4" })`, use `checkPageBreak(height)` helper, same margin (15mm) and yPos tracking. Sections:
    - Title: "Scan Report" (24pt bold)
    - Metadata: folder path, file pattern, scan date, generated timestamp (10pt normal, gray text color 100)
    - Summary: "Summary" heading (14pt bold), bullet items for total files, error files, warning files, clean files, total errors, total warnings (10pt normal, indented margin+5)
    - File Details: "File Details" heading (14pt bold). For each file: file name (11pt bold, indented margin+5), relative path below in gray (9pt), then if problems exist: each problem as "  [severity] Ln {line}, Col {column}: {message}" (9pt normal). Use `checkPageBreak(15 + file.problems.length * 4)` before each file block. Skip problems detail for clean files (just show "Clean" status).
    - Return `new Uint8Array(doc.output("arraybuffer"))`.

    **Export hook (`use-scan-export.ts`):** Export `useScanExport()`. Uses `useState` for `isExporting` and `exportError`. Returns `{ isExporting, exportError, exportCsv, exportJson, exportPdf, exportClipboardText, exportClipboardMarkdown }`. Each export function: gets `scanResult` from `useExplorerStore`, if null returns early. For file exports (CSV, JSON, PDF): calls the formatter, then calls `exportService.saveTextFile` or `exportService.saveBinaryFile` with appropriate filename (`scan-report-YYYY-MM-DD.csv/json/pdf`) and filter. For clipboard exports: calls the formatter, then calls `writeText` from `@tauri-apps/plugin-clipboard-manager`, shows success toast via `showToast`. Wraps each in try/catch with `setIsExporting` and error handling (same pattern as `useExport.ts`).

    **`scan-file-row.tsx` component (per D-13, D-15):** Export `ScanFileRow`. Props: `file: ScanFileResult`, `scanRoot: string`, `onFileClick: (filePath: string) => void`. Renders an expandable row:
    - Collapsed state: status icon (red CircleAlert for "error", amber TriangleAlert for "warning", green CircleCheck for "clean" from lucide-react), file name (text-sm font-semibold truncate), relative path (text-xs text-muted-foreground), error count, warning count, encoding label. Chevron icon (ChevronRight/ChevronDown) on the left to toggle expansion. The entire row is clickable to expand/collapse (not to open the file). A separate "Open" button or double-click opens the file.
    - Expanded state: shows child rows for each problem, using the same layout as `ProblemRow` from `problem-row.tsx` (severity icon, line:column, message). Each problem row is styled identically to the existing ProblemRow (indent, monospace line:column, truncated message).
    - Double-click on the file row calls `onFileClick(file.filePath)` to open the file as a regular tab per D-15.
    - Keyboard accessible: tabIndex=0, Enter toggles expansion, role="row".
    - Row styling matches the problems-panel row pattern: `flex items-center gap-2 px-4 py-1.5 hover:bg-accent cursor-pointer text-sm`.

    **`scan-results-header.tsx` component (per D-13, D-16):** Export `ScanResultsHeader`. Props: `result: ScanSummary`, `showIssuesOnly: boolean`, `onToggleFilter: () => void`. Layout follows `file-content-header.tsx` pattern: `flex items-center gap-2 px-4 h-10 border-b bg-muted/50`.
    - Left section: FileSearch icon + "Scan Results" label + folder name (text-sm font-semibold) + summary badge text ("{totalFiles} files, {totalErrors} errors, {totalWarnings} warnings").
    - Right section (separated by `Separator orientation="vertical" className="h-5"`):
      - Filter toggle button: `Button` variant="ghost" size="sm" with Filter icon. Toggles between showing all files and only files with issues (D-14). Active state: highlighted background.
      - Export dropdown: `DropdownMenu` with `Download` icon trigger button (variant="outline" size="sm"). Items: "CSV (.csv)", "PDF (.pdf)", "JSON (.json)", separator, "Copy as Text" (plain text to clipboard per D-20), "Copy as Markdown" (markdown to clipboard per D-20). Each item calls the corresponding function from `useScanExport()`.

    **`scan-results-tab.tsx` component (per D-12, D-13, D-14):** Export `ScanResultsTab`. Reads `scanResult` from the store. If null, returns null. Renders:
    - `ScanResultsHeader` at the top with filter toggle and export dropdown.
    - `ScrollArea` containing the file list. Uses local state `sortField` (default "status") and `sortDirection` (default "desc"), and `showIssuesOnly` (default false) per D-14.
    - Column headers row: clickable column headers for "File", "Status", "Errors", "Warnings", "Encoding". Clicking toggles sort by that field. Show sort direction indicator (ChevronUp/ChevronDown) on active column.
    - File list: `result.files` filtered by `showIssuesOnly` (if true, filter to files where `status !== "clean"`), sorted by `sortField`/`sortDirection`. Renders `ScanFileRow` for each file.
    - `onFileClick` handler calls `openFile(filePath)` from the store to open the file as a regular tab per D-15.
    - Summary stats above the list: four small stat cards showing total files (FolderOpen icon), errors (CircleAlert red), warnings (TriangleAlert amber), clean (CircleCheck green) with counts.
  </action>
  <verify>
    <automated>cd /Users/elliotlayen/Developer/Monocle && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && npm run test -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/features/explorer/utils/scan-csv-export.ts exports exportScanToCsv that returns a string with CSV header "File Path,Line,Column,Severity,Issue Code,Description,Encoding"
    - src/features/explorer/utils/scan-csv-export.ts includes metadata comments starting with "# Scan Report"
    - src/features/explorer/utils/scan-csv-export.test.ts has 4 test cases and all pass
    - src/features/explorer/utils/scan-json-export.ts exports exportScanToJson returning a JSON string with metadata.folderPath and files array
    - src/features/explorer/utils/scan-json-export.test.ts has 3 test cases and all pass
    - src/features/explorer/utils/scan-clipboard-export.ts exports formatScanAsText and formatScanAsMarkdown
    - src/features/explorer/utils/scan-clipboard-export.test.ts has 4 test cases and all pass
    - formatScanAsMarkdown output contains "# Scan Report" and a markdown table with "| File | Status |"
    - src/features/explorer/utils/scan-pdf-export.ts exports exportScanToPdf returning Promise<Uint8Array>
    - src/features/explorer/hooks/use-scan-export.ts exports useScanExport with exportCsv, exportPdf, exportJson, exportClipboardText, exportClipboardMarkdown
    - src/features/explorer/components/scan-file-row.tsx exports ScanFileRow with expandable row showing problems
    - src/features/explorer/components/scan-results-header.tsx exports ScanResultsHeader with export DropdownMenu containing CSV, PDF, JSON, Copy as Text, Copy as Markdown items
    - src/features/explorer/components/scan-results-tab.tsx exports ScanResultsTab with sortable file list, filter toggle, and ScrollArea
    - ScanResultsTab has showIssuesOnly filter state per D-14
    - ScanFileRow double-click calls onFileClick per D-15
    - `npm run build` succeeds
    - `npm run lint` passes
    - `npm run test -- --run` all tests pass including new export formatter tests
  </acceptance_criteria>
  <done>
    All five export formatters produce correctly structured output (CSV with per-problem rows and metadata comments, JSON with metadata wrapper, PDF with summary + file details, clipboard text as compact block, clipboard markdown as table). Unit tests verify CSV, JSON, and clipboard formatters. The export hook wires each to save dialog or clipboard. The scan results tab displays after scan completion with sortable/filterable file list, expandable problem details, and drill-down to individual files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire results tab into content area via file-tab.tsx and finalize scan-to-results transition</name>
  <files>
    src/features/explorer/components/explorer-shell.tsx,
    src/features/explorer/components/file-content-area.tsx,
    src/features/explorer/components/file-tab.tsx,
    src/features/explorer/store.ts
  </files>
  <read_first>
    src/features/explorer/components/explorer-shell.tsx,
    src/features/explorer/components/file-content-area.tsx,
    src/features/explorer/components/file-tab.tsx,
    src/features/explorer/components/file-tab-bar.tsx,
    src/features/explorer/store.ts,
    src/features/explorer/components/scan-results-tab.tsx,
    src/features/explorer/components/scan-progress-panel.tsx
  </read_first>
  <action>
    **Scan results tab lifecycle (D-12, RESEARCH Open Q2):** When scan completes (`scanStatus` transitions to "completed" or "cancelled"), the progress panel is replaced by the results tab. When the user starts a new scan, the old results tab is automatically cleared (since D-04 only allows one scan at a time, keeping old results alongside progress is confusing). Results are session-only -- never persisted.

    **Content area integration:** Modify `explorer-shell.tsx` to handle four content states:
    1. `scanStatus === "scanning"` -- show `ScanProgressPanel` (from Plan 01) as overlay/top section. If tabs are open, the progress panel appears above the tab content. If no tabs, the progress panel fills the content area.
    2. `scanStatus === "completed" || scanStatus === "cancelled"` AND `scanResult !== null` AND no active file tab is selected -- show `ScanResultsTab` as the main content.
    3. File tabs open and active -- show `FileTabBar` + `FileContentArea` as normal.
    4. No tabs and no scan results -- show `ExplorerEmptyState`.

    The results tab integrates into the tab bar system using a synthetic tab approach. In the store, when scan completes, add a synthetic tab entry to `tabs` with id `scan:results`, `fileName` set to "Scan Results - [folderName]", and a special `isScanResult: true` flag. This allows the tab bar to show the scan results alongside file tabs. The tab can be closed via the normal close button (which calls `clearScanResult`). When `activeTabId === "scan:results"`, `FileContentArea` renders `ScanResultsTab` instead of file content.

    **Store changes:** Add `isScanResult?: boolean` to the `FileTab` interface in types.ts (optional field, only true for scan result tabs). Modify `startScan` completion handler to insert the synthetic scan tab into `tabs` and set `activeTabId` to `"scan:results"`. Modify `closeTab` to call `clearScanResult()` when closing a tab with `id === "scan:results"`. Modify `startScan` to remove any existing `scan:results` tab before starting a new scan (per Open Q2 from RESEARCH).

    **FileContentArea modification:** Add a branch at the top of the render: if `activeTab?.id === "scan:results"`, render `<ScanResultsTab />` instead of the normal file content. The scan results tab gets the full content area (no problems panel, no validation status bar -- those are for individual files).

    **Tab bar icon in `file-tab.tsx`:** Modify the `renderIcon` function in `src/features/explorer/components/file-tab.tsx` to check for scan result tabs. Add a condition: if `tab.isScanResult` is true, render `FileSearch` icon from lucide-react (import it alongside existing FileCode, FileText, Loader2). The check goes before the existing `tab.isLoading` and `tab.isXml` checks. This ensures the scan results tab shows the FileSearch icon in the tab bar instead of a file icon.
  </action>
  <verify>
    <automated>cd /Users/elliotlayen/Developer/Monocle && npm run build 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/features/explorer/types.ts FileTab interface has optional `isScanResult?: boolean` field
    - src/features/explorer/store.ts startScan completion handler creates a synthetic tab with id "scan:results" and sets it as activeTabId
    - src/features/explorer/store.ts closeTab for "scan:results" calls clearScanResult
    - src/features/explorer/store.ts startScan removes existing "scan:results" tab before starting new scan
    - src/features/explorer/components/file-content-area.tsx renders ScanResultsTab when activeTab.id === "scan:results"
    - src/features/explorer/components/file-tab.tsx renderIcon returns FileSearch icon when tab.isScanResult is true
    - src/features/explorer/components/explorer-shell.tsx shows ScanProgressPanel overlay during scanning state
    - `npm run build` succeeds
    - `npm run lint` passes
    - `npm run test -- --run` all tests pass
  </acceptance_criteria>
  <done>
    The scan results tab integrates seamlessly into the tab system. When a scan completes, a "Scan Results" tab appears in the tab bar alongside file tabs with a FileSearch icon. Users can switch between scan results and open files. Closing the results tab clears the scan state. Starting a new scan replaces any existing results tab. The progress-to-results transition is smooth. The full Phase 5 user story is complete: scan a folder, watch progress, browse results, drill into files, and export reports.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scan results -> export formatters | In-memory data transformed to file/clipboard output |
| Export -> filesystem | Generated CSV/PDF/JSON written via Tauri plugin-fs |
| Export -> clipboard | Formatted text written via clipboard-manager plugin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | Export files (CSV/PDF/JSON) | accept | Exports contain file paths and validation issues only, not file content. User explicitly triggers export via save dialog. No sensitive content is included. |
| T-05-06 | Information Disclosure | Clipboard export | accept | User explicitly clicks "Copy as Text/Markdown". Clipboard content is validation metadata, not file content. |
| T-05-07 | Tampering | CSV export injection | mitigate | CSV fields are properly escaped (double-quote wrapping, internal quote doubling). No formula injection risk since fields contain file paths and validation messages, not user-provided input. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. All dependencies from Plan 01 are already installed. |
</threat_model>

<verification>
1. `npm run build` succeeds (TypeScript compiles, Vite builds)
2. `npm run lint` passes with no errors
3. `npm run test -- --run` passes all existing + new tests (CSV, JSON, clipboard export tests)
4. Export formatters produce correctly structured output (verified by unit tests and acceptance criteria)
</verification>

<success_criteria>
After a scan completes, users see a results tab in the tab bar (with FileSearch icon) showing a sortable file list with expandable problem details. They can filter to issues-only, click to drill into individual files, and export results in CSV, PDF, JSON, clipboard text, or clipboard markdown formats. All exports include scan metadata. Unit tests verify CSV, JSON, and clipboard formatter correctness. The results tab integrates into the tab system and can be closed or replaced by a new scan.
</success_criteria>

<output>
Create `.planning/phases/05-bulk-validation-reporting/05-02-SUMMARY.md` when done
</output>
