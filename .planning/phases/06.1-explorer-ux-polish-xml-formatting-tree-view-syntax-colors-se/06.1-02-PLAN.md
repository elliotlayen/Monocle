---
phase: 06.1-explorer-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/explorer/components/file-content-header.tsx
  - src/features/explorer/components/search-bar.tsx
  - src/features/explorer/components/folder-tree-node.tsx
  - src/features/explorer/components/folder-tree.tsx
autonomous: true
requirements:
  - UX-02
  - UX-03
  - UX-05

must_haves:
  truths:
    - "Both segmented controls (Tree/Source and Filename/Content) have identical visual styling"
    - "Both segmented controls have role=group, aria-label, and aria-pressed for accessibility"
    - "Date folders in the tree display formatted dates alongside raw names (e.g. '20251223 (Dec 23, 2025)')"
    - "The dateSortOrder toggle actually reverses the sort order of date-pattern folders"
  artifacts:
    - path: "src/features/explorer/components/file-content-header.tsx"
      provides: "Standardized segmented control with accessibility"
      contains: "role=\"group\""
    - path: "src/features/explorer/components/search-bar.tsx"
      provides: "Standardized segmented control matching header"
      contains: "role=\"group\""
    - path: "src/features/explorer/components/folder-tree-node.tsx"
      provides: "Date folder display with formatted date"
      contains: "formatDateFolder"
    - path: "src/features/explorer/components/folder-tree.tsx"
      provides: "Date sort order applied to sort logic"
      contains: "dateSortOrder"
  key_links:
    - from: "src/features/explorer/components/folder-tree-node.tsx"
      to: "src/features/explorer/utils/date-format.ts"
      via: "import formatDateFolder"
      pattern: 'from "../utils/date-format"'
    - from: "src/features/explorer/components/folder-tree.tsx"
      to: "src/features/explorer/store.ts"
      via: "useShallow selector"
      pattern: "dateSortOrder"
---

<objective>
Standardize both segmented controls to identical styling with proper accessibility, wire the existing formatDateFolder utility into tree node rendering, and connect dateSortOrder to the actual sort logic.

Purpose: Fix three wiring gaps and one consistency issue: (1) segmented controls look different despite serving the same UX pattern, (2) formatDateFolder was implemented and tested in Phase 2 but never connected to the UI, (3) dateSortOrder toggle button exists but the sort comparator ignores it.

Output: Visually consistent segmented controls with ARIA support, formatted date display in folder tree, working date sort toggle.
</objective>

<execution_context>
@/Users/elliotlayen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elliotlayen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-explorer-ux-polish-xml-formatting-tree-view-syntax-colors-se/06.1-RESEARCH.md
@.planning/phases/06.1-explorer-ux-polish-xml-formatting-tree-view-syntax-colors-se/06.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize segmented controls in file-content-header and search-bar</name>
  <files>src/features/explorer/components/file-content-header.tsx, src/features/explorer/components/search-bar.tsx</files>
  <read_first>
    src/features/explorer/components/file-content-header.tsx
    src/features/explorer/components/search-bar.tsx
  </read_first>
  <action>
    Standardize both segmented controls to use identical Tailwind classes and accessibility attributes.

    Target pattern for both controls:

    Container div: className="flex items-center h-7 rounded-md bg-muted border p-0.5" with role="group" and aria-label (use "View mode" for Tree/Source, keep "Search mode" for Filename/Content).

    Each segment button: use raw button elements (not shadcn Button) with className using cn():
    - Base: "h-full px-2.5 flex items-center gap-1.5 text-xs font-medium rounded-sm"
    - Active: "bg-background shadow-sm"
    - Inactive: "text-muted-foreground hover:text-foreground"
    - Each button gets aria-pressed={isActive} attribute.

    In file-content-header.tsx (around lines 49-91):
    - Add p-0.5 to the container div.
    - Add role="group" and aria-label="View mode" to the container.
    - Add font-medium to both segment buttons.
    - Add aria-pressed to both buttons (aria-pressed={tab.viewMode === "tree"} and aria-pressed={tab.viewMode === "source"}).

    In search-bar.tsx (around lines 67-109):
    - Replace the shadcn Button components with raw button elements.
    - Change container from "bg-muted/50" to "bg-muted" and add explicit "h-7".
    - Change segment button classes from "h-6 px-2 text-xs font-semibold" to "h-full px-2.5 ... text-xs font-medium" matching the target pattern.
    - Keep the existing role="group", aria-label="Search mode", and aria-pressed attributes.
    - Remove the Button import if it becomes unused (check if Button is used elsewhere in the file first).
  </action>
  <verify>
    <automated>cd /Users/elliotlayen/Developer/Monocle && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - file-content-header.tsx segmented control container has classes h-7, bg-muted, border, p-0.5
    - file-content-header.tsx segmented control container has role="group" and aria-label="View mode"
    - file-content-header.tsx segment buttons have aria-pressed attribute
    - search-bar.tsx segmented control container has classes h-7, bg-muted (not bg-muted/50), border, p-0.5
    - search-bar.tsx segment buttons use font-medium (not font-semibold)
    - search-bar.tsx segment buttons have h-full (not h-6)
    - Both controls use the same active state class: bg-background shadow-sm
    - npm run build and npm run lint pass
  </acceptance_criteria>
  <done>Both segmented controls (Tree/Source and Filename/Content) render with identical container and segment styling. Both have role="group", aria-label, and aria-pressed for accessibility.</done>
</task>

<task type="auto">
  <name>Task 2: Wire formatDateFolder to tree nodes and connect dateSortOrder to sort logic</name>
  <files>src/features/explorer/components/folder-tree-node.tsx, src/features/explorer/components/folder-tree.tsx</files>
  <read_first>
    src/features/explorer/components/folder-tree-node.tsx
    src/features/explorer/components/folder-tree.tsx
    src/features/explorer/utils/date-format.ts
    src/features/explorer/store.ts
  </read_first>
  <action>
    Part A -- Wire formatDateFolder in folder-tree-node.tsx:

    Add import: import { formatDateFolder } from "../utils/date-format";

    In the renderName function (around lines 155-165), after the existing node.name span, conditionally show the formatted date for directory nodes that are not source-level nodes. Call formatDateFolder(node.name) for directory nodes (node.isDir === true). If the result has a non-null formatted field, render it in a span with className="text-xs text-muted-foreground ml-1" showing the formatted date in parentheses.

    Determine whether a node is a source node by checking if the node represents a root folder source (isSource prop or equivalent). The PATTERNS.md shows isSource is already a variable in scope -- only format date for non-source directory nodes since source nodes are root paths, not date folders.

    Part B -- Wire dateSortOrder in folder-tree.tsx:

    In the useShallow selector (around lines 22-38), add dateSortOrder to the destructured state.

    In the sort comparator (around lines 75-80), after the directory-first check (a.isDir !== b.isDir), add date-pattern detection: if both nodes have 8-digit names (/^\d{8}$/.test(name)), sort them based on dateSortOrder. When dateSortOrder === "newest", use b.name.localeCompare(a.name) (reverse). When dateSortOrder === "oldest", use a.name.localeCompare(b.name) (normal alphabetical, which matches chronological for YYYYMMDD). For non-date folders, keep the existing a.name.localeCompare(b.name).

    The dateSortOrder state and toggleDateSort action already exist in the store (line 34 and line 394). The toggle button already exists in explorer-sidebar.tsx. This task only needs to consume the state in the sort logic.
  </action>
  <verify>
    <automated>cd /Users/elliotlayen/Developer/Monocle && npm run build && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - folder-tree-node.tsx imports formatDateFolder from "../utils/date-format"
    - folder-tree-node.tsx renderName function calls formatDateFolder for directory nodes
    - folder-tree-node.tsx renders formatted date in a span with class text-muted-foreground when formatDateFolder returns a non-null formatted value
    - folder-tree.tsx useShallow selector includes dateSortOrder
    - folder-tree.tsx sort comparator contains /^\d{8}$/ regex test
    - folder-tree.tsx sort comparator uses dateSortOrder to determine sort direction for date folders
    - npm run build passes
    - npm run test passes (existing date-format tests still green)
  </acceptance_criteria>
  <done>Date folders display formatted dates (e.g., "20251223 (Dec 23, 2025)") in the tree sidebar. The date sort toggle button actually changes the sort order of date-pattern folders between newest-first and oldest-first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Folder names to date parser | Directory names from file system parsed as dates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-03 | Spoofing | folder-tree-node.tsx date display | accept | formatDateFolder validates 8-digit names and rejects invalid dates (month/day range checks). Folder names are read from the file system, not user input. Display-only -- no security impact. |
| T-06.1-04 | Information Disclosure | segmented control ARIA labels | accept | ARIA labels contain only UI state descriptions ("View mode", "Search mode"), no sensitive data. |
</threat_model>

<verification>
- npm run build succeeds
- npm run lint passes
- npm run test -- --run passes (all existing tests still green)
- Segmented controls are visually identical (manual)
- Date folders show formatted dates (manual)
- Date sort toggle reverses sort order (manual)
</verification>

<success_criteria>
- Both segmented controls use identical Tailwind classes and ARIA attributes
- Date folders display formatted dates inline
- Date sort toggle changes actual sort order of 8-digit-named folders
- All existing tests pass, build succeeds, lint clean
</success_criteria>

<output>
Create `.planning/phases/06.1-explorer-ux-polish-xml-formatting-tree-view-syntax-colors-se/06.1-02-SUMMARY.md` when done
</output>
