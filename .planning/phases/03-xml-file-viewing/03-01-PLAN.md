---
phase: 03-xml-file-viewing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/commands/explorer.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src/features/explorer/types.ts
  - src/services/tauri.ts
  - src/features/explorer/services/explorer-service.ts
  - src/features/explorer/store.ts
  - src/features/explorer/utils/file-size-format.ts
  - src/features/explorer/utils/tab-disambiguator.ts
  - src/features/explorer/utils/file-size-format.test.ts
  - src/features/explorer/utils/tab-disambiguator.test.ts
  - src/lib/monaco-xml-loader.ts
  - src/features/explorer/components/file-tab-bar.tsx
  - src/features/explorer/components/file-tab.tsx
  - src/features/explorer/components/file-content-area.tsx
  - src/features/explorer/components/file-content-header.tsx
  - src/features/explorer/components/xml-source-view.tsx
  - src/features/explorer/components/explorer-shell.tsx
  - src/features/explorer/components/explorer-empty-state.tsx
  - src/features/explorer/components/folder-tree-node.tsx
autonomous: true
requirements:
  - BRWS-05
  - BRWS-06

must_haves:
  truths:
    - "User can click a file in the sidebar tree and it opens in a new tab with syntax-highlighted source view"
    - "User can open multiple files and switch between tabs"
    - "Clicking an already-open file switches to its existing tab instead of creating a duplicate"
    - "Closing a tab switches to the adjacent tab or shows the empty state if last tab"
    - "Tab labels show filename only, with parent folder disambiguator for duplicates"
    - "Content header shows filename, file size, and a tree/source toggle"
    - "Empty state message says 'Click a file in the sidebar to open it' when sources exist but no file is open"
  artifacts:
    - path: "src-tauri/src/commands/explorer.rs"
      provides: "read_file_cmd Rust command"
      contains: "read_file_cmd"
    - path: "src/features/explorer/store.ts"
      provides: "Tab state management"
      contains: "openFile"
    - path: "src/features/explorer/components/file-tab-bar.tsx"
      provides: "Horizontal scrollable tab bar"
      exports: ["FileTabBar"]
    - path: "src/features/explorer/components/xml-source-view.tsx"
      provides: "Monaco XML source view"
      exports: ["XmlSourceView"]
    - path: "src/features/explorer/components/file-content-area.tsx"
      provides: "Content area container"
      exports: ["FileContentArea"]
  key_links:
    - from: "src/features/explorer/components/folder-tree-node.tsx"
      to: "src/features/explorer/store.ts"
      via: "onFileClick -> openFile store action"
      pattern: "openFile"
    - from: "src/features/explorer/store.ts"
      to: "src/features/explorer/services/explorer-service.ts"
      via: "explorerService.readFile"
      pattern: "readFile"
    - from: "src/features/explorer/services/explorer-service.ts"
      to: "src/services/tauri.ts"
      via: "tauri.readFile"
      pattern: "read_file_cmd"
    - from: "src/features/explorer/components/explorer-shell.tsx"
      to: "src/features/explorer/components/file-tab-bar.tsx"
      via: "conditional render when tabs open"
      pattern: "FileTabBar"
---

<objective>
Deliver the core file viewing slice: clicking a file in the sidebar opens it in a tabbed source view with Monaco XML syntax highlighting. After this plan, users can open multiple files in tabs, switch between them, close tabs, and see syntax-highlighted file content.

Purpose: This is the primary end-to-end vertical slice for Phase 3 -- it connects the existing sidebar tree (Phase 2) to a new tabbed file viewer, completing the read path from filesystem through Rust backend to the user's screen.

Output: Working tabbed source viewer with backend file reading, tab management, and Monaco XML syntax highlighting. All infrastructure for Plans 02 and 03 is in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-xml-file-viewing/03-CONTEXT.md
@.planning/phases/03-xml-file-viewing/03-RESEARCH.md
@.planning/phases/03-xml-file-viewing/03-PATTERNS.md
@.planning/phases/03-xml-file-viewing/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts from existing codebase. Executor should use directly. -->

From src/features/explorer/types.ts:
```typescript
export interface FolderSource { id: string; path: string; label: string; tag: string; favorites: string[]; }
export type TreeNodeType = "source" | "client" | "date" | "file";
export type LoadState = "idle" | "loading" | "loaded" | "error";
export interface TreeNode { id: string; path: string; name: string; type: TreeNodeType; children: TreeNode[] | null; loadState: LoadState; childCount?: number; isDir: boolean; isFavorite?: boolean; }
export interface DirEntry { name: string; isDir: boolean; path: string; }
```

From src/services/tauri.ts:
```typescript
async function invokeCommand<T>(command: string, args?: Record<string, unknown>): Promise<T>;
export const tauri = { listDirectory, cancelDirectory, checkPathReachable, toggleFavorite, ... };
```

From src/features/explorer/services/explorer-service.ts:
```typescript
export const explorerService = { listDirectory, cancelDirectory, checkPathReachable, toggleFavorite };
```

From src/features/explorer/store.ts (ExplorerStore interface):
```typescript
interface ExplorerStore {
  folderSources: FolderSource[]; treeNodes: Map<string, TreeNode>; expandedIds: Set<string>;
  activeOperations: Map<string, string>; filterText: string; dateSortOrder: "newest" | "oldest";
  sidebarOpen: boolean; sidebarWidth: number;
  loadSources, expandNode, collapseNode, cancelLoad, setFilterText, toggleDateSort, toggleFavorite, setSidebarOpen, setSidebarWidth, reorderSources, saveSources
}
```

From src-tauri/src/commands/explorer.rs:
```rust
#[derive(Serialize)] #[serde(rename_all = "camelCase")]
pub struct DirEntry { pub name: String, pub is_dir: bool, pub path: String }
pub async fn list_directory_cmd(path: String, operation_id: String, explorer_state: State<'_, ExplorerState>) -> Result<Vec<DirEntry>, String>
```

From src/features/explorer/components/folder-tree-node.tsx:
```typescript
interface FolderTreeNodeProps {
  node: TreeNode; depth: number; sourceId: string; isExpanded: boolean;
  onExpand: (nodeId: string) => void; onCollapse: (nodeId: string) => void;
  onCancel: (nodeId: string) => void; onToggleFavorite: (sourceId: string, clientName: string) => void;
}
```

From src/features/explorer/components/explorer-shell.tsx:
```typescript
interface ExplorerShellProps { onHome: () => void; onOpenSettings: () => void; }
```

From src/features/notifications/store.ts:
```typescript
export function showToast(toast: Omit<Toast, "id">): string;
// Toast type: { type: "info" | "success" | "error" | "warning" | "update"; title: string; message?: string; duration?: number; }
```

From src/lib/monaco-sql-loader.ts (pattern to replicate for XML):
```typescript
import { loader } from "@monaco-editor/react";
let monacoSqlLoadPromise: Promise<void> | null = null;
export const ensureMonacoSqlLoaded = () => { ... Promise.all([editor.api, sql.contribution.js]) ... };
```

From src/features/schema-graph/components/sql-code-block.tsx (Monaco pattern):
```typescript
import Editor from "@monaco-editor/react";
const [isMonacoReady, setIsMonacoReady] = useState(false);
const resolvedTheme = useResolvedTheme();
const monacoTheme = resolvedTheme === "dark" ? "vs-dark" : "vs";
```
</interfaces>
</context>

## Phase Goal

**As a** support analyst, **I want to** open and inspect XML integration files with syntax highlighting and a tabbed interface, **so that** I can quickly review file contents without leaving the application.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend file reading + store tab management + utilities</name>
  <files>
    src-tauri/Cargo.toml,
    src-tauri/src/commands/explorer.rs,
    src-tauri/src/commands/mod.rs,
    src-tauri/src/lib.rs,
    src-tauri/capabilities/default.json,
    src/features/explorer/types.ts,
    src/services/tauri.ts,
    src/features/explorer/services/explorer-service.ts,
    src/features/explorer/store.ts,
    src/features/explorer/utils/file-size-format.ts,
    src/features/explorer/utils/tab-disambiguator.ts,
    src/features/explorer/utils/file-size-format.test.ts,
    src/features/explorer/utils/tab-disambiguator.test.ts
  </files>
  <read_first>
    src-tauri/src/commands/explorer.rs,
    src-tauri/src/commands/mod.rs,
    src-tauri/src/lib.rs,
    src-tauri/Cargo.toml,
    src-tauri/capabilities/default.json,
    src/features/explorer/types.ts,
    src/services/tauri.ts,
    src/features/explorer/services/explorer-service.ts,
    src/features/explorer/store.ts,
    src/features/explorer/utils/date-format.ts,
    src/features/explorer/utils/date-format.test.ts,
    src/features/explorer/store-integration.test.ts
  </read_first>
  <behavior>
    - formatFileSize(0) returns "0 B"
    - formatFileSize(1024) returns "1 KB"
    - formatFileSize(1536) returns "1.5 KB"
    - formatFileSize(1048576) returns "1 MB"
    - formatFileSize(512) returns "512 B"
    - disambiguateTabNames with unique filenames returns each filename unchanged
    - disambiguateTabNames with two tabs sharing "file1.xml" appends parent folder: "file1.xml - 20251223" and "file1.xml - 20251224"
    - disambiguateTabNames with single-file returns just filename
    - Store openFile creates a new tab with viewMode "source", isLoading transitions false->true->false
    - Store openFile on already-open file path just switches activeTabId (no duplicate tab)
    - Store closeTab removes tab, switches to right neighbor, or left if rightmost, or null if last
    - Store closeOtherTabs removes all tabs except the specified one
    - Store closeAllTabs sets tabs to empty array and activeTabId to null
    - Store setViewMode updates the specified tab's viewMode
    - Store setScrollPosition updates the specified tab's scrollPosition for the given view
  </behavior>
  <action>
    **Rust backend (per D-04, D-05):**

    Install clipboard-manager plugin: add `tauri-plugin-clipboard-manager = "2"` to `[dependencies]` in `src-tauri/Cargo.toml`.

    In `src-tauri/src/commands/explorer.rs`, add a `FileContent` struct with `#[derive(Serialize)] #[serde(rename_all = "camelCase")]` containing fields `content: String` and `size: u64`. Add `read_file_cmd` async Tauri command following the `list_directory_cmd` pattern: accepts `path: String`, uses `tokio::time::timeout(Duration::from_secs(30), tokio::task::spawn_blocking(...))`. Inside the blocking closure, call `std::fs::metadata(&path)` for `size` (using `.len()`) and `std::fs::read_to_string(&path)` for content. Return `Result<FileContent, String>`. Match on the timeout result with three arms: `Ok(Ok(fc)) => fc`, `Ok(Err(e)) => Err(...)`, `Err(_) => Err("File read timed out after 30 seconds")`. No cancellation token needed (unlike directory listing).

    In `src-tauri/src/commands/mod.rs`, add `read_file_cmd` to the `pub use explorer::{ ... }` block.

    In `src-tauri/src/lib.rs`, add `.plugin(tauri_plugin_clipboard_manager::init())` to the plugin chain (after `tauri_plugin_fs::init()`). Add `read_file_cmd` to the `use commands::{ ... }` import block and to the `generate_handler![ ... ]` macro invocation.

    In `src-tauri/capabilities/default.json`, add `"opener:allow-open-path"` and `"clipboard-manager:allow-write-text"` to the `permissions` array.

    **TypeScript types (in src/features/explorer/types.ts):**

    Add `ViewMode` type alias: `"source" | "tree"`.
    Add `FileContent` interface: `{ content: string; size: number }`.
    Add `FileTab` interface with fields: `id: string` (file path as unique ID), `filePath: string`, `fileName: string` (display name with disambiguator), `content: string`, `fileSize: number` (bytes), `viewMode: ViewMode` (default: `"source"` per D-02), `scrollPosition: { source: number; tree: number }`, `isXml: boolean`, `parseError: boolean`, `isLoading: boolean`.

    **Service layer:**

    In `src/services/tauri.ts`, import `FileContent` from `@/features/explorer/types`. Add `readFile: (path: string) => invokeCommand<FileContent>("read_file_cmd", { path })` to the explorer commands section.

    In `src/features/explorer/services/explorer-service.ts`, import `FileContent` from types. Add `readFile: (path: string): Promise<FileContent> => tauri.readFile(path)` to the `explorerService` object.

    **Store extension (in src/features/explorer/store.ts):**

    Import `FileTab, ViewMode, FileContent` from types. Import `showToast` from `@/features/notifications/store`. Add tab state to ExplorerStore interface: `tabs: FileTab[]`, `activeTabId: string | null`. Add tab actions: `openFile: (filePath: string) => Promise<void>`, `closeTab: (tabId: string) => void`, `closeOtherTabs: (tabId: string) => void`, `closeAllTabs: () => void`, `setActiveTab: (tabId: string) => void`, `setViewMode: (tabId: string, mode: ViewMode) => void`, `setScrollPosition: (tabId: string, view: ViewMode, position: number) => void`.

    Initialize `tabs: []` and `activeTabId: null`.

    `openFile` action: check if tab exists with matching filePath -- if yes, call `setActiveTab` and return (per D-14). Otherwise, extract filename from path using `path.split(/[/\\]/).pop()`. Determine `isXml` by checking if filename ends with `.xml` (case-insensitive). Create a new `FileTab` with `id: filePath`, `isLoading: true`, `viewMode: "source"`, `scrollPosition: { source: 0, tree: 0 }`, `content: ""`, `fileSize: 0`, `parseError: false`. Add to `tabs` array and set as `activeTabId`. Then call `explorerService.readFile(filePath)` in try/catch. On success, update the tab's `content`, `fileSize`, `isLoading: false`. On error, remove the tab, show error toast via `showToast({ type: "error", title: "Failed to read file", message: "Check that the file still exists and is accessible", duration: 5000 })`. After any tab change, recompute display names using the `disambiguateTabNames` utility and update all `fileName` fields.

    `closeTab` action: find tab index. If it is the active tab, switch to right neighbor (index + 1), or left neighbor (index - 1) if rightmost, or `null` if last tab. Remove tab from array. Recompute disambiguated names.

    `closeOtherTabs`: filter tabs to keep only the specified tabId. Set activeTabId to that tabId. Recompute disambiguated names.

    `closeAllTabs`: set `tabs: []`, `activeTabId: null`.

    `setActiveTab`: set `activeTabId` to the given tabId.

    `setViewMode`: find tab, update its `viewMode` field.

    `setScrollPosition`: find tab, update `scrollPosition[view]` to the given position number.

    **Utilities:**

    Create `src/features/explorer/utils/file-size-format.ts` exporting `formatFileSize(bytes: number): string`. Handle: 0 returns "0 B", below 1024 returns "{n} B", then "KB", "MB", "GB" using 1024-based divisions. Show one decimal place only when not a whole number for KB+.

    Create `src/features/explorer/utils/tab-disambiguator.ts` exporting `disambiguateTabNames(tabs: { filePath: string }[]): Map<string, string>` (key: filePath, value: display name). Count filename occurrences. For duplicates, append ` - {parentFolder}` where parentFolder is the last path segment before the filename. Split on both `/` and `\\` for cross-platform support.

    **Tests:**

    Create `src/features/explorer/utils/file-size-format.test.ts` following the `date-format.test.ts` vitest pattern: import from vitest, test the behaviors listed above.

    Create `src/features/explorer/utils/tab-disambiguator.test.ts` following same pattern: test unique filenames, duplicate filenames with different parents, single file.
  </action>
  <verify>
    <automated>cd /Users/elliotlayen/Developer/Monocle && npx vitest run src/features/explorer/utils/file-size-format.test.ts src/features/explorer/utils/tab-disambiguator.test.ts --reporter=verbose && cd src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/commands/explorer.rs contains `pub async fn read_file_cmd(path: String) -> Result<FileContent, String>`
    - src-tauri/src/commands/explorer.rs contains `pub struct FileContent` with `#[serde(rename_all = "camelCase")]`
    - src-tauri/src/lib.rs contains `tauri_plugin_clipboard_manager::init()`
    - src-tauri/src/lib.rs generate_handler contains `read_file_cmd`
    - src-tauri/Cargo.toml contains `tauri-plugin-clipboard-manager`
    - src-tauri/capabilities/default.json contains `opener:allow-open-path` and `clipboard-manager:allow-write-text`
    - src/features/explorer/types.ts exports ViewMode, FileTab, FileContent
    - src/services/tauri.ts contains `readFile: (path: string) => invokeCommand<FileContent>("read_file_cmd", { path })`
    - src/features/explorer/services/explorer-service.ts contains `readFile` method
    - src/features/explorer/store.ts contains `tabs: FileTab[]`, `activeTabId`, `openFile`, `closeTab`, `closeOtherTabs`, `closeAllTabs`, `setActiveTab`, `setViewMode`, `setScrollPosition`
    - src/features/explorer/utils/file-size-format.ts exports `formatFileSize`
    - src/features/explorer/utils/tab-disambiguator.ts exports `disambiguateTabNames`
    - All vitest tests in file-size-format.test.ts and tab-disambiguator.test.ts pass
    - cargo check succeeds in src-tauri
  </acceptance_criteria>
  <done>Rust read_file_cmd compiled, clipboard plugin registered, TypeScript types/services/store extended with tab management, utility functions created with passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Tab bar, content area, source view, and shell wiring</name>
  <files>
    src/lib/monaco-xml-loader.ts,
    src/features/explorer/components/file-tab-bar.tsx,
    src/features/explorer/components/file-tab.tsx,
    src/features/explorer/components/file-content-area.tsx,
    src/features/explorer/components/file-content-header.tsx,
    src/features/explorer/components/xml-source-view.tsx,
    src/features/explorer/components/explorer-shell.tsx,
    src/features/explorer/components/explorer-empty-state.tsx,
    src/features/explorer/components/folder-tree-node.tsx
  </files>
  <read_first>
    src/lib/monaco-sql-loader.ts,
    src/features/schema-graph/components/sql-code-block.tsx,
    src/features/explorer/components/explorer-shell.tsx,
    src/features/explorer/components/explorer-empty-state.tsx,
    src/features/explorer/components/folder-tree-node.tsx,
    src/features/explorer/components/explorer-sidebar.tsx,
    src/features/explorer/store.ts,
    src/features/explorer/types.ts,
    src/hooks/use-resolved-theme.ts,
    .planning/phases/03-xml-file-viewing/03-UI-SPEC.md,
    .planning/phases/03-xml-file-viewing/03-PATTERNS.md
  </read_first>
  <action>
    **Monaco XML loader (src/lib/monaco-xml-loader.ts):**

    Replicate the pattern from `src/lib/monaco-sql-loader.ts`. Export `ensureMonacoXmlLoaded`. Use `monacoXmlLoadPromise` singleton. Import `loader` from `@monaco-editor/react`. Dynamic import `monaco-editor/esm/vs/editor/editor.api` and `monaco-editor/esm/vs/basic-languages/xml/xml.contribution.js`. Call `loader.config({ monaco: localMonaco })` in the .then handler. No intellisense registration (unlike SQL loader). Guard with `typeof window === "undefined"` check.

    **FileTabBar (src/features/explorer/components/file-tab-bar.tsx):**

    Select `tabs`, `activeTabId`, `setActiveTab`, `closeTab` from useExplorerStore with useShallow. Render a horizontal scrollable container: `flex items-center h-9 bg-muted border-b overflow-x-auto overflow-y-hidden` with hidden scrollbar CSS (`scrollbar-width: none`, webkit scrollbar `display: none` via a className or inline style). Map over tabs to render `FileTab` components. Pass `isActive`, `onActivate`, `onClose`, `onMiddleClick` (close on middle mouse button via onMouseDown with `e.button === 1`).

    **FileTab (src/features/explorer/components/file-tab.tsx):**

    Props: `tab: FileTab`, `isActive: boolean`, `onActivate`, `onClose`, `onCloseOthers`, `onCloseAll`. Wrap in ContextMenu with items: Close, Close Others, Close All (with ContextMenuSeparator before Close Others). Wrap the tab label area in Tooltip showing the full `tab.filePath`. Tab styling per UI-SPEC: `flex items-center gap-1.5 px-4 h-full text-sm border-r border-border cursor-pointer whitespace-nowrap group`. Active tab: `bg-background border-b-2 border-b-primary`. Inactive: `bg-muted text-muted-foreground hover:bg-accent hover:text-accent-foreground`. Show file icon: `FileCode` (h-3.5 w-3.5) for XML, `FileText` for non-XML. When `tab.isLoading`, show `Loader2` with `animate-spin` instead of file icon. Tab label: `text-sm truncate max-w-[160px]` showing `tab.fileName`. Close button (X icon): `h-5 w-5 rounded-sm opacity-0 group-hover:opacity-100 hover:bg-muted-foreground/20`. Always visible on active tab (conditional `opacity-100`). Close button calls `onClose` with `e.stopPropagation()`.

    **FileContentArea (src/features/explorer/components/file-content-area.tsx):**

    Select `tabs`, `activeTabId` from store. Find active tab from tabs array using activeTabId. Render `FileContentHeader` + content body. Content body: if `activeTab.isLoading`, show Skeleton placeholders (three `Skeleton` elements: `h-4 w-3/4`, `h-4 w-1/2`, `h-4 w-2/3` stacked with `gap-2`, centered). If `activeTab.viewMode === "source"`, render `XmlSourceView`. If `activeTab.viewMode === "tree"`, render a placeholder div with text "Tree view coming soon" (Plan 02 replaces this with XmlTreeView). Container: `flex flex-col flex-1 overflow-hidden`.

    **FileContentHeader (src/features/explorer/components/file-content-header.tsx):**

    Select `setViewMode` from store. Props: `tab: FileTab`. Container: `flex items-center gap-2 px-4 h-10 border-b bg-muted/50`. Left section: file icon (FileCode/FileText, h-4 w-4 text-muted-foreground), filename (text-sm font-semibold truncate), file size via `formatFileSize(tab.fileSize)` (text-xs text-muted-foreground) -- show `--` if `tab.isLoading`. Center-right: segmented control for tree/source toggle. Container: `flex items-center h-7 rounded-md bg-muted border`. Each segment: `px-2.5 h-full flex items-center gap-1.5 text-xs rounded-sm cursor-pointer`. Active segment: `bg-background shadow-sm`. Inactive: `text-muted-foreground hover:text-foreground`. TreePine icon for "Tree", Code icon for "Source". Hide toggle entirely when `!tab.isXml` (per D-03). Disable tree segment (`opacity-50 cursor-not-allowed`) when `tab.parseError`, with tooltip "Unable to parse XML". Right section: Separator (vertical, h-5) followed by action button placeholders -- for now render Copy (copy path) and ClipboardCopy (copy content) as ghost icon buttons (h-7 w-7) with tooltips. Wire click handlers to no-op functions temporarily -- Plan 03 replaces these with the real `useFileActions` hook. Import `Copy`, `ClipboardCopy` from lucide-react.

    **XmlSourceView (src/features/explorer/components/xml-source-view.tsx):**

    Follow `sql-code-block.tsx` pattern. Props: `content: string`, `isXml: boolean`, `tabId: string`, `scrollPosition: number`, `onScrollChange: (position: number) => void`. Use `ensureMonacoXmlLoaded` (not SQL). `isMonacoReady` state. `useResolvedTheme` for theme. Monaco options per UI-SPEC: `readOnly: true`, `domReadOnly: true`, `minimap: { enabled: false }`, `lineNumbers: "on"`, `lineNumbersMinChars: 3`, `folding: true` (D-06), `glyphMargin: false`, `scrollBeyondLastLine: false`, `wordWrap: "on"`, `automaticLayout: true`, `fontSize: 13`, `lineHeight: 20`, `fontFamily` monospace stack, `overviewRulerLanes: 0`, `renderLineHighlight: "line"`, `contextmenu: false`, `padding: { top: 8, bottom: 8 }`, `scrollbar: { alwaysConsumeMouseWheel: false }`, `ariaLabel: "File source view"`. Language: `isXml ? "xml" : "plaintext"`. Key the Editor component by `tabId` to force remount on tab switch (avoiding memory leaks per Pitfall 2). On editor mount (`onMount` callback), restore scroll position via `editor.setScrollTop(scrollPosition)`. Set up a scroll listener to call `onScrollChange` with the new scrollTop value (use `editor.onDidScrollChange`). Container: `flex-1 overflow-hidden`. Fallback while Monaco loads: `pre` with monospace styling showing raw content.

    **ExplorerShell modification (src/features/explorer/components/explorer-shell.tsx):**

    Add `tabs` and `activeTabId` to the useShallow selector. Import `FileTabBar` and `FileContentArea`. Determine `hasOpenTabs = tabs.length > 0`. Change the content div to `flex-1 flex flex-col overflow-hidden relative`. Conditionally render: if `hasOpenTabs`, show `FileTabBar` then `FileContentArea`; else show `ExplorerEmptyState`. The `SidebarToggle` stays above both branches (it is positioned absolutely).

    **ExplorerEmptyState modification (src/features/explorer/components/explorer-empty-state.tsx):**

    Per D-15: add a third message state. When `hasSources` is true, change the body text from "Expand a source in the sidebar to browse folders." to "Click a file in the sidebar to open it." (per copywriting contract in UI-SPEC). Remove the `useEffect` for `loadSources` -- ExplorerSidebar already handles this on mount. The `loadSources` import and selector can remain for the `folderSources` check.

    **FolderTreeNode modification (src/features/explorer/components/folder-tree-node.tsx):**

    Add `onFileClick: (filePath: string) => void` to the `FolderTreeNodeProps` interface. For file nodes (where `isFile` is true): change `cursor-default` to `cursor-pointer`, add `hover:bg-muted`. Set `onClick` to call `onFileClick(node.path)`. This mirrors how directories call `handleToggle` on click. The parent `FolderTree` component must pass `onFileClick` down -- it should call `useExplorerStore.getState().openFile(filePath)`. Read `folder-tree.tsx` to find where `FolderTreeNode` is rendered and add the `onFileClick` prop there too.
  </action>
  <verify>
    <automated>cd /Users/elliotlayen/Developer/Monocle && npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/monaco-xml-loader.ts exports ensureMonacoXmlLoaded
    - src/features/explorer/components/file-tab-bar.tsx exports FileTabBar
    - src/features/explorer/components/file-tab.tsx exports FileTab
    - src/features/explorer/components/file-content-area.tsx exports FileContentArea
    - src/features/explorer/components/file-content-header.tsx exports FileContentHeader with tree/source segmented control
    - src/features/explorer/components/xml-source-view.tsx exports XmlSourceView with language="xml" and folding: true
    - src/features/explorer/components/explorer-shell.tsx conditionally renders FileTabBar + FileContentArea when tabs.length > 0
    - src/features/explorer/components/explorer-empty-state.tsx body text reads "Click a file in the sidebar to open it." when sources exist
    - src/features/explorer/components/folder-tree-node.tsx file nodes have cursor-pointer, hover:bg-muted, and onClick calling onFileClick
    - npm run build succeeds (TypeScript compiles)
    - npm run lint passes
  </acceptance_criteria>
  <done>Clicking a file in the sidebar tree opens it in a new tab with Monaco XML syntax highlighting. Multiple tabs work with close, switch, and duplicate prevention. Content header shows filename, file size, and tree/source toggle. Empty state shows context-aware message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Rust | File path string from user-clicked tree node crosses into Rust read_file_cmd |
| Filesystem -> Rust | File content from potentially untrusted network share enters application |
| File content -> DOMParser | Raw file content will be parsed by DOMParser in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | read_file_cmd path input | accept | Path comes from directory listing (Phase 2) which reads from OS filesystem. User controls folder sources. Tauri FS scope not restrictive but this is a desktop app where user has full filesystem access anyway. |
| T-03-02 | Denial of Service | read_file_cmd large file | mitigate | 30-second timeout on spawn_blocking prevents indefinite hang. Memory bounded by file size which user controls (their own network share). |
| T-03-03 | Information Disclosure | clipboard write | accept | Clipboard write is user-initiated (click action). Desktop app, user has full access to file content already. |
| T-03-SC | Tampering | npm install @tauri-apps/plugin-clipboard-manager | mitigate | Package passed slopcheck [OK]. Official Tauri plugins-workspace monorepo. Cargo crate is official tauri-plugin-clipboard-manager. |
</threat_model>

<verification>
1. `cargo check` in src-tauri succeeds
2. `npm run build` succeeds (TypeScript compiles, no errors)
3. `npm run lint` passes
4. `npx vitest run src/features/explorer/utils/file-size-format.test.ts src/features/explorer/utils/tab-disambiguator.test.ts` -- all tests pass
5. `npm run test -- --run` -- full test suite passes
</verification>

<success_criteria>
- User can click any file node in the sidebar tree and it opens in a new tab
- Source view displays file content with XML syntax highlighting (for .xml files) or plain text (other files)
- Multiple tabs can be open simultaneously with independent scroll positions
- Clicking an already-open file switches to its tab (no duplicates per D-14)
- Tab close button works, switching to adjacent tab
- Tab context menu offers Close, Close Others, Close All
- Content header shows filename, file size, and tree/source segmented control
- Empty state shows "Click a file in the sidebar to open it" when sources configured
- All tests pass, lint clean, TypeScript compiles
</success_criteria>

<output>
Create `.planning/phases/03-xml-file-viewing/03-01-SUMMARY.md` when done
</output>
