---
phase: 06-search
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/features/explorer/components/search-bar.tsx
  - src/features/explorer/components/search-controls-row.tsx
  - src/features/explorer/components/search-results.tsx
  - src/features/explorer/components/search-result-group.tsx
  - src/features/explorer/components/search-result-row.tsx
  - src/features/explorer/components/search-progress.tsx
  - src/features/explorer/components/explorer-sidebar.tsx
  - src/features/explorer/hooks/use-search.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03]
must_haves:
  truths:
    - "User can type in the search bar and filter files by name in real time"
    - "User can toggle between Filename and Content modes via segmented control"
    - "User can select search scope (Selected folder, This source, All sources) from dropdown"
    - "User can execute a content search and see results stream progressively into the sidebar"
    - "User can cancel an in-progress search and keep partial results"
    - "Search results are grouped by parent folder with file counts"
  artifacts:
    - path: "src/features/explorer/components/search-bar.tsx"
      provides: "Unified search input with Filename/Content toggle"
      contains: "SearchBar"
    - path: "src/features/explorer/components/search-controls-row.tsx"
      provides: "Scope dropdown, file pattern, Search Files button"
      contains: "SearchControlsRow"
    - path: "src/features/explorer/components/search-results.tsx"
      provides: "Container for search results replacing folder tree"
      contains: "SearchResults"
    - path: "src/features/explorer/components/search-result-group.tsx"
      provides: "Collapsible folder group in search results"
      contains: "SearchResultGroup"
    - path: "src/features/explorer/components/search-result-row.tsx"
      provides: "Single file entry in search results"
      contains: "SearchResultRow"
    - path: "src/features/explorer/components/search-progress.tsx"
      provides: "Inline progress indicator during active search"
      contains: "SearchProgress"
    - path: "src/features/explorer/hooks/use-search.ts"
      provides: "Event subscription hook for search progress and results"
      contains: "useSearch"
  key_links:
    - from: "src/features/explorer/components/explorer-sidebar.tsx"
      to: "src/features/explorer/components/search-bar.tsx"
      via: "component import and render"
      pattern: "<SearchBar"
    - from: "src/features/explorer/hooks/use-search.ts"
      to: "src/services/events.ts"
      via: "searchResultHub and searchProgressHub subscription"
      pattern: "useTauriEvent"
    - from: "src/features/explorer/components/search-results.tsx"
      to: "src/features/explorer/store.ts"
      via: "useExplorerStore for searchResults"
      pattern: "useExplorerStore"
---

<objective>
Build the complete search UI: unified search bar with mode toggle, content search controls, streaming results display with folder grouping, and progress indicator. After this plan, users can filter files by name (SRCH-01), execute content searches with scope selection (SRCH-02, SRCH-03), see results stream in progressively, and cancel long searches. Implements D-01 through D-21 (search bar, toggle, scope, results display). Per D-13: scope auto-updates with tree selection and also switches the searchScope value to "folder" when the user clicks a tree node while idle.

Purpose: Deliver the core search user experience as a complete vertical slice. Users can find files by name or content.

Output: Six new components (SearchBar, SearchControlsRow, SearchResults, SearchResultGroup, SearchResultRow, SearchProgress), one new hook (useSearch), modified ExplorerSidebar to wire everything together.
</objective>

<execution_context>
@/Users/elliotlayen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elliotlayen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-search/06-CONTEXT.md
@.planning/phases/06-search/06-RESEARCH.md
@.planning/phases/06-search/06-PATTERNS.md
@.planning/phases/06-search/06-UI-SPEC.md
@.planning/phases/06-search/06-01-SUMMARY.md

@src/features/explorer/components/explorer-sidebar.tsx
@src/features/explorer/store.ts
@src/features/explorer/types.ts
@src/features/explorer/hooks/use-scan.ts
@src/features/explorer/components/scan-progress-panel.tsx
@src/features/explorer/components/folder-tree.tsx
@src/services/events.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SearchBar, SearchControlsRow, scope auto-update, and sidebar wiring for filename filtering</name>
  <files>
    src/features/explorer/components/search-bar.tsx,
    src/features/explorer/components/search-controls-row.tsx,
    src/features/explorer/components/explorer-sidebar.tsx
  </files>
  <read_first>
    src/features/explorer/components/explorer-sidebar.tsx (the filter input at lines 103-111 is being replaced),
    src/features/explorer/store.ts (search state and actions from Plan 01 Task 2),
    src/features/explorer/types.ts (SearchMode, SearchScope types),
    .planning/phases/06-search/06-UI-SPEC.md (SearchBar layout, SearchControlsRow layout, Interaction Contracts),
    .planning/phases/06-search/06-PATTERNS.md (search-bar.tsx and search-controls-row.tsx analog patterns)
  </read_first>
  <action>
    CREATE search-bar.tsx (per D-01, D-02, D-03, D-05):
    Export `SearchBar` component. Import `useShallow` from zustand, `Search` and `X` from lucide-react, `Input` from `@/components/ui/input`, `Button` from `@/components/ui/button`, `Tooltip`/`TooltipContent`/`TooltipProvider`/`TooltipTrigger` from `@/components/ui/tooltip`, `cn` from `@/lib/utils`, `useExplorerStore` from `../store`.

    Consume from store via useShallow: `searchMode`, `searchQuery`, `setSearchMode`, `setSearchQuery`.

    Layout per UI-SPEC "SearchBar" section:
    - Outer container: `flex items-center gap-2`
    - Left section: relative wrapper containing Search icon (absolute positioned left-2.5, h-4 w-4 text-muted-foreground) and shadcn Input. Input: `h-8 text-sm pl-8 flex-1`, value bound to `searchQuery`, onChange calls `setSearchQuery(e.target.value)`. Placeholder: `"Search files..."` when mode is filename, `"Search file contents..."` when mode is content (per UI-SPEC Copywriting Contract). When searchQuery is non-empty, show an X clear button at right edge of input (`absolute right-2 top-1/2 -translate-y-1/2 h-5 w-5`) that calls `setSearchQuery("")`. Add `pr-8` to input when clear button is visible.
    - Right section: segmented toggle. Container: `flex items-center rounded-md border bg-muted/50 p-0.5`. Two Button elements with variant="ghost", each `h-6 px-2 text-xs font-semibold rounded-sm`. Active segment: `bg-background shadow-sm`. Inactive: `bg-transparent hover:bg-transparent`. Labels: "Filename" and "Content". OnClick: call `setSearchMode("filename")` or `setSearchMode("content")`.
    - Add role="group" aria-label="Search mode" on toggle container. Each button: aria-pressed matching active state.
    - Add role="searchbox" and aria-label="Search files" on the Input.
    - Wrap toggle buttons in TooltipProvider. Filename tooltip: "Search filenames (Cmd+F)". Content tooltip: "Search file contents (Cmd+Shift+F)" (per UI-SPEC Copywriting Contract).
    - On Enter key in the input when mode is "content", call the `onSearchExecute` prop (passed from parent) to trigger content search.

    CREATE search-controls-row.tsx (per D-04, D-09, D-10, D-11, D-12, D-13, D-14):
    Export `SearchControlsRow` component. Props: `selectedNodePath: string | null`, `selectedNodeName: string | null`, `selectedSourceLabel: string | null`, `isSearching: boolean`, `onSearch: () => void`. Import `Select`/`SelectContent`/`SelectItem`/`SelectTrigger`/`SelectValue` from `@/components/ui/select`, `Button` from `@/components/ui/button`, `Input` from `@/components/ui/input`, `useExplorerStore` from `../store`.

    Consume from store via useShallow: `searchScope`, `setSearchScope`, `searchFilePattern`, `setSearchFilePattern`, `searchStatus`.

    Layout per UI-SPEC "SearchControlsRow" section:
    - Container: `flex items-center gap-2 px-3 pb-2`
    - Scope dropdown: shadcn Select, trigger `h-8 text-xs flex-1 min-w-0`. Value bound to `searchScope`. Options:
      - "folder": label = selectedNodePath ? `Folder: ${selectedNodeName}` : "Selected folder" (D-12)
      - "source": label = selectedSourceLabel ? `Source: ${selectedSourceLabel}` : "This source"
      - "all": label = "All sources"
    - Scope dropdown disabled when `searchStatus !== "idle"` (per D-13: scope locked during/after search).
    - File pattern input: Input, `h-8 text-xs w-20`, value bound to `searchFilePattern`, onChange calls `setSearchFilePattern`. Placeholder: "*.xml".
    - Search Files button: Button variant="default" size="sm" `h-8 px-3 text-xs font-semibold`. Label: "Search Files". Disabled when: `!selectedNodePath && searchScope !== "all"` (per D-11), or when `isSearching` is true. OnClick calls `onSearch` prop.
    - aria-label="Search scope" on Select component.

    MODIFY explorer-sidebar.tsx:
    - Remove the existing filter input div (lines 103-111 containing the Search icon and "Filter clients..." Input).
    - Import and render `SearchBar` in its place.
    - Import and render `SearchControlsRow` below SearchBar, but only when `searchMode === "content"` (per D-04). Container wraps both SearchBar and SearchControlsRow within the border-b header area.
    - Add to useShallow selector: `searchMode`, `searchStatus`, `searchResults`, `searchErrors`, `searchScope`, `setSearchScope` (needed for conditional tree/results rendering in Task 2 and scope auto-update).
    - Derive props for SearchControlsRow: selectedNodePath from the currently selected tree node (use treeNodes map + a selectedNodeId if the store has one, or derive from the last interacted folder path via `lastInteractedFolderPath`). selectedSourceLabel from the folder source that contains the selected node. isSearching from `searchStatus === "searching"`.
    - The tree body section (`<FolderTree />` at line 115) remains for now -- Task 2 will add conditional rendering.
    - Remove `filterText` and `setFilterText` from the useShallow selector since SearchBar now handles this internally via setSearchQuery.
    - Per D-13 (scope auto-updates with tree selection): Add a useEffect in explorer-sidebar.tsx that watches `selectedNodePath` (or `lastInteractedFolderPath`). When the selected node changes AND `searchStatus === "idle"`, call `setSearchScope("folder")`. This ensures that when a user clicks a different folder in the tree before running a search, the scope automatically switches to "Selected folder" reflecting their new selection. This only triggers when idle -- scope is locked during/after search.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - src/features/explorer/components/search-bar.tsx exports SearchBar component
    - SearchBar renders an Input with Search icon and a two-segment toggle (Filename/Content)
    - SearchBar Input has role="searchbox" and aria-label
    - Toggle container has role="group" and aria-label="Search mode"
    - Each toggle button has aria-pressed attribute
    - Placeholder text changes based on searchMode ("Search files..." vs "Search file contents...")
    - X clear button appears when searchQuery is non-empty
    - src/features/explorer/components/search-controls-row.tsx exports SearchControlsRow component
    - SearchControlsRow contains Select (scope), Input (file pattern), and Button (Search Files)
    - Search Files button is disabled when selectedNodePath is null and scope is not "all"
    - SearchControlsRow scope Select is disabled when searchStatus is not "idle" (D-13 scope lock)
    - explorer-sidebar.tsx no longer contains "Filter clients..." placeholder text
    - explorer-sidebar.tsx renders SearchBar component
    - explorer-sidebar.tsx conditionally renders SearchControlsRow when searchMode is "content"
    - explorer-sidebar.tsx has useEffect that calls setSearchScope("folder") when selected node changes and searchStatus is "idle" (D-13)
    - `npm run build` succeeds
    - `npm run lint` passes
    - `npm run test -- --run` passes
  </acceptance_criteria>
  <done>SearchBar replaces old filter input with Filename/Content toggle. SearchControlsRow shows scope dropdown, file pattern, and Search Files button in content mode. Scope dropdown is locked during/after search per D-13. Scope auto-updates to "folder" when tree selection changes while idle per D-13. Filename mode instantly filters the tree via store.setSearchQuery -> setFilterText chain. Users can toggle modes and set search parameters.</done>
</task>

<task type="auto">
  <name>Task 2: SearchResults, SearchResultGroup, SearchResultRow, SearchProgress, useSearch hook, and sidebar conditional rendering</name>
  <files>
    src/features/explorer/components/search-results.tsx,
    src/features/explorer/components/search-result-group.tsx,
    src/features/explorer/components/search-result-row.tsx,
    src/features/explorer/components/search-progress.tsx,
    src/features/explorer/hooks/use-search.ts,
    src/features/explorer/components/explorer-sidebar.tsx
  </files>
  <read_first>
    src/features/explorer/components/explorer-sidebar.tsx (as modified by Task 1),
    src/features/explorer/hooks/use-scan.ts (exact analog for useSearch hook),
    src/features/explorer/components/scan-progress-panel.tsx (exact analog for SearchProgress),
    src/features/explorer/components/folder-tree.tsx (ScrollArea pattern for results container),
    src/features/explorer/store.ts (search state/actions from Plan 01),
    src/services/events.ts (searchResultHub, searchProgressHub from Plan 01),
    src/features/explorer/types.ts (SearchResultFile, SearchErrorFile, SearchProgressPayload),
    .planning/phases/06-search/06-UI-SPEC.md (SearchResults layout, folder group, file result row, error group, progress indicator, no-selection prompt, no-results),
    .planning/phases/06-search/06-PATTERNS.md (search-results.tsx, search-result-group.tsx, search-result-row.tsx, search-progress.tsx analog patterns)
  </read_first>
  <action>
    CREATE use-search.ts (mirrors use-scan.ts exactly per PATTERNS.md):
    Export `useSearch` hook. Import `useCallback` from react, `useShallow` from zustand, `useTauriEvent`, `searchResultHub`, `searchProgressHub` from `@/services/events`, `useExplorerStore` from `../store`, types `SearchResultFile`, `SearchProgressPayload` from `../types`.

    Consume from store via useShallow: `startContentSearch`, `cancelContentSearch`, `clearSearchResults`, `updateSearchProgress`, `appendSearchResult`, `searchStatus`, `searchProgress`, `searchResults`, `searchErrors`, `searchSummary`, `searchQuery`, `searchScope`, `searchFilePattern`, `searchMode`, `searchOperationId`.

    Create `handleProgress` callback: calls `updateSearchProgress(payload)`. Only update if `payload.operationId` matches `searchOperationId` (per RESEARCH pitfall 5 -- stale events).
    Create `handleResult` callback: validates `payload.operationId` matches `searchOperationId` before calling `appendSearchResult(payload)`. Note: SearchResultPayload from the backend includes `operationId` -- the frontend type SearchResultFile does not include it, so extract and check operationId from the raw event payload before passing the rest to appendSearchResult.

    Subscribe via `useTauriEvent(searchProgressHub.subscribe, handleProgress)` and `useTauriEvent(searchResultHub.subscribe, handleResult)`.

    Export a `resolveSearchScope` function (or include inline) that takes `searchScope`, `selectedNodePath`, `selectedSourcePath`, `folderSources` and returns `{ paths: string[], label: string } | null`. Logic: scope "folder" returns `[selectedNodePath]` with label `Folder: {nodeName}`. Scope "source" returns `[selectedSourcePath]` with label `Source: {sourceLabel}`. Scope "all" returns `folderSources.map(s => s.path)` with label `"All sources"`. Returns null if required path is missing.

    Return all state and actions for consumers.

    CREATE search-progress.tsx (per D-19, mirrors scan-progress-panel.tsx):
    Export `SearchProgress` component. Import `Loader2`, `X` from lucide-react, `Button` from `@/components/ui/button`. Props: `progress: SearchProgressPayload | null`, `onCancel: () => void`.

    Layout per UI-SPEC "Progress indicator" section:
    - Container: `flex items-center gap-2 px-4 py-2 border-b bg-muted/20`
    - Spinner: Loader2 icon `h-4 w-4 animate-spin text-muted-foreground`
    - Text: `text-xs text-muted-foreground flex-1` showing `"Searching... {filesScanned} of {totalFiles} files"`
    - Stop Search button: Button variant="outline" size="sm" `h-6 px-2 text-xs` with X icon `h-3 w-3 mr-1` and label "Stop Search"
    - role="status" aria-live="polite" on container

    CREATE search-result-group.tsx (per D-17):
    Export `SearchResultGroup` component. Props: `folderPath: string`, `files: SearchResultFile[]`, `defaultExpanded: boolean`, `onFileClick: (filePath: string) => void`, `isError?: boolean`.

    Layout per UI-SPEC "Folder group" section:
    - Header: `flex items-center gap-2 px-4 py-1 cursor-pointer hover:bg-accent/50`. ChevronDown/ChevronRight (h-3.5 w-3.5 text-muted-foreground) based on expanded state. Folder icon (h-3.5 w-3.5 text-muted-foreground). Folder name `text-xs font-semibold truncate` -- extract folder name from folderPath. Spacer. File count `text-xs text-muted-foreground`.
    - For error groups (isError=true): header text uses `text-destructive`, AlertCircle icon instead of Folder, label "Errors ({N} files)" (per D-26, UI-SPEC "Error group").
    - Children: render SearchResultRow for each file when expanded. Sorted alphabetically by fileName (per D-20).
    - role="group" aria-label="{folderPath}" on container. Header: aria-expanded.
    - Default expanded state controlled by `defaultExpanded` prop: normal groups default expanded, error groups default collapsed (per UI-SPEC).

    CREATE search-result-row.tsx (per D-17):
    Export `SearchResultRow` component. Props: `file: SearchResultFile | SearchErrorFile`, `onClick: () => void`, `isError?: boolean`.

    Layout per UI-SPEC "File result row" section:
    - Container: `flex items-center gap-2 px-4 py-1 pl-8 cursor-pointer hover:bg-accent text-sm`. role="button".
    - Normal file: FileText icon (h-4 w-4 text-muted-foreground), filename `truncate flex-1`, match count `text-xs text-muted-foreground flex-shrink-0` showing "{N} matches" or "{N} match" (singular).
    - Error file (isError=true): AlertCircle icon (h-4 w-4 text-destructive), filename `truncate`, error message `text-xs text-destructive truncate` (per D-26, UI-SPEC "Error result row").
    - aria-label="{filename}, {N} matches" for normal files, aria-label="{filename}, error: {message}" for errors.

    CREATE search-results.tsx (per D-16, D-17, D-18, D-21):
    Export `SearchResults` component. Import ScrollArea from `@/components/ui/scroll-area`, `X` from lucide-react, `Button` from `@/components/ui/button`. Props: `results: SearchResultFile[]`, `errors: SearchErrorFile[]`, `summary: SearchSummary | null`, `progress: SearchProgressPayload | null`, `status: SearchStatus`, `scopeLabel: string`, `onCancel: () => void`, `onClear: () => void`, `onFileClick: (filePath: string) => void`.

    Layout per UI-SPEC "SearchResults" section:
    - role="region" aria-label="Search results" aria-live="polite"
    - Summary header: `flex items-center gap-2 px-4 py-2 border-b bg-muted/30`. Stats text `text-xs text-muted-foreground flex-1`. During search: "{filesScanned} files scanned". Completed: "{N} files, {M} matches in {scopeLabel}" (per D-17). Cancelled: add "(stopped)". Clear button: Button variant="ghost" size="icon" `h-5 w-5` with X icon h-3 w-3, tooltip "Clear search" (per UI-SPEC). Calls onClear.
    - SearchProgress (rendered inline when status is "searching")
    - Group results by parentFolder. Create a Map grouping SearchResultFile[] by parentFolder. Render each group as SearchResultGroup with defaultExpanded=true. Error group rendered last with isError=true, defaultExpanded=false.
    - No results state: when status is "completed" and results is empty and errors is empty, show "No matches found" heading and "No files matched \"{query}\" in {scopeLabel}" body (per UI-SPEC Copywriting Contract). Container: `flex flex-col items-center justify-center py-12 px-8`. Text: `text-sm text-muted-foreground text-center`.
    - ScrollArea wrapping the groups list.

    MODIFY explorer-sidebar.tsx:
    - Import SearchResults, SearchProgress, useSearch hook.
    - In the tree body section (currently just `<FolderTree />`), add conditional rendering:
      - If `searchMode === "content"` AND (`searchStatus !== "idle"` OR `searchResults.length > 0` OR `searchErrors.length > 0`): render `<SearchResults />` instead of `<FolderTree />`
      - If `searchMode === "content"` AND `searchStatus === "idle"` AND no results AND no selected folder path (D-11): render the "Select a folder in the tree to set search scope" prompt. Container: `flex flex-col items-center justify-center h-full px-8`. Text: `text-sm text-muted-foreground text-center`. Render FolderTree underneath (or show prompt instead -- per D-11 the prompt appears where the tree would be when no folder is selected, but D-16 says tree visible until search executed; resolve: show tree always in content mode before search starts, show prompt inline above tree when no selection).
      - Otherwise: render `<FolderTree />`
    - Wire the onSearch callback for SearchControlsRow: use resolveSearchScope from useSearch to get paths and label, then call startContentSearch(paths, label).
    - Wire SearchResults props from store state via useSearch hook.
    - Wire onFileClick to `openFile` store action (existing action -- opens file in tab).
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - src/features/explorer/hooks/use-search.ts exports useSearch hook
    - useSearch subscribes to searchResultHub and searchProgressHub via useTauriEvent
    - useSearch validates operationId on incoming events before updating store
    - src/features/explorer/components/search-progress.tsx exports SearchProgress with Loader2 spinner and "Stop Search" button
    - src/features/explorer/components/search-result-group.tsx exports SearchResultGroup with collapsible folder header
    - SearchResultGroup renders ChevronDown/ChevronRight based on expanded state
    - src/features/explorer/components/search-result-row.tsx exports SearchResultRow with FileText icon and match count
    - SearchResultRow shows singular "match" for count of 1
    - src/features/explorer/components/search-results.tsx exports SearchResults with summary header, grouped results, and clear button
    - SearchResults shows "No matches found" when completed with zero results
    - SearchResults summary header shows "{N} files, {M} matches in {scopeLabel}" format
    - explorer-sidebar.tsx conditionally renders SearchResults vs FolderTree based on searchMode and searchStatus
    - explorer-sidebar.tsx shows "Select a folder" prompt when in content mode with no selection (D-11)
    - explorer-sidebar.tsx wires onSearch to startContentSearch with resolved scope paths
    - `npm run build` succeeds
    - `npm run lint` passes
    - `npm run test -- --run` passes
  </acceptance_criteria>
  <done>Complete search UI is functional. Users can execute content searches, see streaming results grouped by folder, cancel searches, and clear results to return to the tree. Filename filtering works via the search bar toggle. Scope selection drives which folders are searched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> scope resolution | User-selected scope translated to filesystem paths -- must resolve within configured sources |
| Backend events -> store | Search result events ingested into store must be filtered by operationId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Spoofing | search event operationId | mitigate | useSearch hook validates event.operationId matches active searchOperationId before store update |
| T-06-06 | Denial of Service | result list rendering | accept | D-21 says no result limit; sidebar uses ScrollArea for virtualized scrolling. Thousands of results may slow rendering but cancel button provides escape hatch |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed. All shadcn components already present. |
</threat_model>

<verification>
1. `npm run build` -- TypeScript compiles with all new components
2. `npm run lint` -- no lint errors
3. `npm run test -- --run` -- existing tests still pass
4. Manual: Toggle between Filename and Content modes in sidebar
5. Manual: Type in search bar, see tree filter in filename mode
6. Manual: Execute content search, see streaming results
</verification>

<success_criteria>
- SearchBar replaces old filter input with mode toggle (D-01, D-02)
- Filename mode filters tree in real time (SRCH-01, D-03, D-05)
- Content mode shows controls row with scope and file pattern (D-04, D-09, D-14)
- Content search executes and streams results (SRCH-02, D-19)
- Search scope selection works (SRCH-03, D-09, D-10, D-12)
- Scope auto-updates to "folder" when tree selection changes while idle (D-13)
- Scope locked during/after search (D-13)
- Results grouped by folder (D-17), sorted alphabetically (D-20)
- Progress indicator shows during search with cancel (D-19)
- No-selection prompt shown when appropriate (D-11)
- No-results message shown when search completes empty
- Clear button restores tree view (D-16)
</success_criteria>

<output>
Create `.planning/phases/06-search/06-02-SUMMARY.md` when done
</output>
