---
phase: 01-explorer-shell-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/schema-graph/store.ts
  - src/features/explorer/store-integration.test.ts
  - src/features/explorer/components/explorer-shell.tsx
  - src/features/explorer/components/explorer-nav-bar.tsx
  - src/features/explorer/components/explorer-empty-state.tsx
  - src/features/connection/components/home-screen.tsx
  - src/App.tsx
autonomous: false
requirements:
  - BRWS-09

must_haves:
  truths:
    - "User can click Integration Explorer on the home screen and enter a new full-screen mode"
    - "A top navigation bar appears with Monocle branding, Home button, and Settings button"
    - "The explorer layout shows a centered empty state with getting-started content and Open Settings CTA"
    - "User can return to the home screen by clicking the Home button in the nav bar"
    - "Pressing Cmd+E from the home screen enters Explorer mode"
    - "Entering and exiting Explorer does not affect Schema Graph state"
  artifacts:
    - path: "src/features/explorer/components/explorer-shell.tsx"
      provides: "Full-screen layout container with nav bar and content area"
      exports: ["ExplorerShell"]
    - path: "src/features/explorer/components/explorer-nav-bar.tsx"
      provides: "Horizontal nav bar with branding, Home and Settings buttons"
      exports: ["ExplorerNavBar"]
    - path: "src/features/explorer/components/explorer-empty-state.tsx"
      provides: "Centered getting-started content with FolderSync icon and Open Settings CTA"
      exports: ["ExplorerEmptyState"]
    - path: "src/features/explorer/store-integration.test.ts"
      provides: "Unit tests for explorer mode state transitions"
      min_lines: 40
    - path: "src/features/schema-graph/store.ts"
      provides: "Extended mode union type with explorer, plus enter/exit actions"
      contains: "enterExplorerMode"
    - path: "src/features/connection/components/home-screen.tsx"
      provides: "Integration Explorer button with FolderSync icon and Cmd+E shortcut"
      contains: "FolderSync"
    - path: "src/App.tsx"
      provides: "Explorer mode conditional rendering, Cmd+E keyboard shortcut, showHome exclusion"
      contains: "isExplorerMode"
  key_links:
    - from: "src/App.tsx"
      to: "src/features/schema-graph/store.ts"
      via: "useSchemaStore selector for enterExplorerMode/exitExplorerMode"
      pattern: "enterExplorerMode|exitExplorerMode"
    - from: "src/App.tsx"
      to: "src/features/explorer/components/explorer-shell.tsx"
      via: "conditional render when isExplorerMode is true"
      pattern: "isExplorerMode.*ExplorerShell"
    - from: "src/features/connection/components/home-screen.tsx"
      to: "src/App.tsx"
      via: "onEnterExplorer callback prop"
      pattern: "onEnterExplorer"
---

## Phase Goal

**As a** Monocle user, **I want to** enter an Integration Explorer mode from the home screen, **so that** I see the layout shell that will host all explorer functionality in future phases.

<objective>
Deliver the complete Explorer Shell and Navigation vertical slice: mode state, three new components, App.tsx wiring, home screen button, and keyboard shortcut. After this plan, users can enter and exit Explorer mode from the home screen.

Purpose: Establish the Integration Explorer as a first-class mode alongside Schema Graph and Canvas, providing the shell that Phases 2-10 will populate with browsing, validation, search, and analytics features.
Output: 7 files (3 new components, 1 test file, 3 modified files). Explorer mode fully functional with empty state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-explorer-shell-navigation/01-CONTEXT.md
@.planning/phases/01-explorer-shell-navigation/01-RESEARCH.md
@.planning/phases/01-explorer-shell-navigation/01-PATTERNS.md
@.planning/phases/01-explorer-shell-navigation/01-UI-SPEC.md
@.planning/phases/01-explorer-shell-navigation/01-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/features/schema-graph/store.ts (line 64):
  Current mode type: mode: "connected" | "canvas"
  Canvas mode actions at lines 120-125: enterCanvasMode, exitCanvasMode
  Store interface: SchemaStore (lines 41-134)
  Initial state factory: createInitialSchemaState (exported)

From src/features/schema-graph/store.test.ts (lines 1-17):
  Test setup pattern: vi.mock for schema-service and settings-service
  State reset pattern: useSchemaStore.setState(createInitialSchemaState())

From src/App.tsx:
  Mode derived state (line 96): const isCanvasMode = mode === "canvas"
  showHome conditional (line 357): !schema && mode !== "canvas" && (!isConnected || !serverConnection)
  Keyboard handler (lines 266-282): useEffect with mod key detection for Cmd+K
  Store selector (lines 28-82): useShallow selector for store fields and actions
  Canvas callbacks (lines 163-166): handleEnterCanvasMode with useCallback
  Conditional render (lines 384-429): showHome ternary with HomeScreen vs ReactFlowProvider

From src/features/connection/components/home-screen.tsx:
  Props (lines 6-11): HomeScreenProps with onOpenConnectionModal, onOpenSettings, onOpenAbout, onEnterCanvasMode
  Canvas button (lines 54-66): Button variant="outline" with PenTool icon and Cmd+K badge
  Icon imports (line 1): import { Server, Settings, Info, PenTool } from "lucide-react"
  modKey detection (line 15): const modKey = navigator.userAgent...

From src/features/toolbar/components/toolbar.tsx:
  Container CSS (line 484): relative z-20 flex items-center gap-3 px-3 py-2 bg-background border-b border-border
  Branding (lines 487-491): JetBrains Mono fontFamily, font-semibold text-base
  Tooltip button pattern (lines 951-965): TooltipProvider > Tooltip > TooltipTrigger > Button
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store extension with TDD -- add explorer mode to Zustand store</name>
  <files>src/features/explorer/store-integration.test.ts, src/features/schema-graph/store.ts</files>
  <read_first>
    - src/features/schema-graph/store.ts (full file -- understand mode type at line 64, SchemaStore interface lines 41-134, enterCanvasMode/exitCanvasMode at lines 1077-1106, createInitialSchemaState export)
    - src/features/schema-graph/store.test.ts (first 55 lines -- understand test setup pattern with vi.mock and beforeEach)
    - .planning/phases/01-explorer-shell-navigation/01-PATTERNS.md (store.ts section -- exact analog for enter/exit pattern)
    - .planning/phases/01-explorer-shell-navigation/01-RESEARCH.md (Pattern 1: Mode State Extension section and Open Question 1)
  </read_first>
  <behavior>
    - Test 1: enterExplorerMode sets mode to "explorer"
    - Test 2: exitExplorerMode sets mode to "connected"
    - Test 3: enterExplorerMode preserves existing schema, isConnected, connectionInfo, serverConnection state (state fields are unchanged after enter+exit round trip)
    - Test 4: mode "explorer" is distinct from "connected" and "canvas" (entering explorer from connected, then exiting returns to connected)
  </behavior>
  <action>
    RED phase: Create src/features/explorer/store-integration.test.ts following the exact pattern from src/features/schema-graph/store.test.ts lines 1-54. Import useSchemaStore and createInitialSchemaState from @/features/schema-graph/store. Mock schema-service and settings-service using the same vi.mock calls. Write 4 test cases per the behavior block. Run tests -- all 4 must fail (enterExplorerMode/exitExplorerMode do not exist yet).

    GREEN phase: Modify src/features/schema-graph/store.ts:
    1. Extend the mode union type at line 64 from "connected" | "canvas" to "connected" | "canvas" | "explorer" (per D-01)
    2. Add enterExplorerMode and exitExplorerMode to the SchemaStore interface (after exitCanvasMode around line 125)
    3. Implement enterExplorerMode as: () => set({ mode: "explorer" }) -- ONLY sets mode, does NOT touch any other state (per RESEARCH.md Open Question 1 recommendation and Pitfall 3: no state reset)
    4. Implement exitExplorerMode as: () => set({ mode: "connected" }) -- returns to connected mode
    Run tests -- all 4 must pass.

    Validation: npm run test -- --run
  </action>
  <verify>
    <automated>npx vitest run src/features/explorer/store-integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/features/explorer/store-integration.test.ts exists with 4 test cases
    - src/features/schema-graph/store.ts line 64 contains mode: "connected" | "canvas" | "explorer"
    - src/features/schema-graph/store.ts contains enterExplorerMode: () => set({ mode: "explorer" })
    - src/features/schema-graph/store.ts contains exitExplorerMode: () => set({ mode: "connected" })
    - npx vitest run src/features/explorer/store-integration.test.ts exits 0 with 4 passing tests
    - npm run build exits 0 (no type errors from mode union change)
  </acceptance_criteria>
  <done>Store has "explorer" in the mode union, enterExplorerMode and exitExplorerMode actions implemented, 4 passing unit tests covering enter, exit, state preservation, and mode distinctness</done>
</task>

<task type="auto">
  <name>Task 2: Explorer components, App.tsx wiring, HomeScreen button, and Cmd+E shortcut</name>
  <files>
    src/features/explorer/components/explorer-shell.tsx,
    src/features/explorer/components/explorer-nav-bar.tsx,
    src/features/explorer/components/explorer-empty-state.tsx,
    src/features/connection/components/home-screen.tsx,
    src/App.tsx
  </files>
  <read_first>
    - src/App.tsx (full file -- understand store selector at lines 28-82, mode conditionals at line 96, keyboard handler at lines 266-282, showHome at line 357, conditional render at lines 384-429)
    - src/features/connection/components/home-screen.tsx (full file -- understand props interface, button order, icon imports, modKey pattern)
    - src/features/toolbar/components/toolbar.tsx (lines 484-500 for container CSS, lines 487-491 for branding, lines 951-965 for tooltip button pattern)
    - .planning/phases/01-explorer-shell-navigation/01-UI-SPEC.md (full file -- visual contracts for all components)
    - .planning/phases/01-explorer-shell-navigation/01-PATTERNS.md (full file -- exact code patterns for all 6 files)
    - .planning/phases/01-explorer-shell-navigation/01-CONTEXT.md (decisions D-01 through D-13)
  </read_first>
  <action>
    Create three new component files under src/features/explorer/components/ and modify two existing files. All components are presentational (props in, UI out, no direct store access) per CLAUDE.md.

    1. Create src/features/explorer/components/explorer-nav-bar.tsx (per D-02, D-03):
       - ExplorerNavBarProps interface with onHome: () => void and onOpenSettings: () => void
       - Container div with exact CSS from toolbar.tsx line 484: relative z-20 flex items-center gap-3 px-3 py-2 bg-background border-b border-border
       - Left: "Monocle" span with fontFamily: "'JetBrains Mono', monospace" and className font-semibold text-base (per UI-SPEC branding)
       - Spacer: div with flex-1
       - Right group: div with flex items-center gap-2
       - Home button: shadcn Button variant="outline" size="sm" className="h-9 px-2" with Home icon (w-4 h-4), wrapped in Tooltip with content "Home", onClick={onHome}
       - Settings button: same pattern with Settings icon, Tooltip content "Settings", onClick={onOpenSettings}
       - Both buttons inside a single TooltipProvider wrapper
       - Imports: Button from @/components/ui/button, Tooltip/TooltipContent/TooltipProvider/TooltipTrigger from @/components/ui/tooltip, Home and Settings from lucide-react

    2. Create src/features/explorer/components/explorer-empty-state.tsx (per D-05, D-06):
       - ExplorerEmptyStateProps interface with onOpenSettings: () => void
       - Outer container: div with flex items-center justify-center h-full
       - Inner wrapper: div with flex flex-col items-center gap-3 text-center
       - Icon: FolderSync from lucide-react with className w-10 h-10 text-muted-foreground mb-1 (per D-11, UI-SPEC)
       - Heading: h2 with text-xl font-semibold, text "Integration Explorer" (per UI-SPEC copywriting)
       - Description: p with text-sm text-muted-foreground max-w-sm, exact text "Browse, search, and validate XML integration files from your configured folder sources." (per UI-SPEC copywriting)
       - CTA: shadcn Button default variant with className mt-6, Settings icon (w-4 h-4) + text "Open Settings", onClick={onOpenSettings}
       - Imports: Button from @/components/ui/button, FolderSync and Settings from lucide-react

    3. Create src/features/explorer/components/explorer-shell.tsx (per D-06):
       - ExplorerShellProps interface with onHome: () => void and onOpenSettings: () => void
       - Container: div with flex flex-col h-screen
       - Renders ExplorerNavBar with onHome and onOpenSettings props
       - main element with flex-1 overflow-hidden containing ExplorerEmptyState with onOpenSettings
       - Imports: ExplorerNavBar from ./explorer-nav-bar, ExplorerEmptyState from ./explorer-empty-state

    4. Modify src/features/connection/components/home-screen.tsx (per D-09, D-10, D-11, D-12):
       - Add FolderSync to the lucide-react import (alongside Server, Settings, Info, PenTool)
       - Add onEnterExplorer?: () => void to HomeScreenProps interface
       - Add onEnterExplorer to the destructured props
       - Add Integration Explorer button AFTER the Canvas Mode button and BEFORE the Settings button
       - Button follows exact pattern of Canvas Mode button: variant="outline" className="w-full h-12 justify-between px-4"
       - Left span: flex items-center gap-3 with FolderSync (w-5 h-5) and text "Integration Explorer"
       - Right kbd: text-xs text-muted-foreground bg-muted px-2 py-1 rounded with {modKey}+E

    5. Modify src/App.tsx:
       - Add enterExplorerMode and exitExplorerMode to the useShallow store selector (alongside existing enterCanvasMode/exitCanvasMode)
       - Add derived state: const isExplorerMode = mode === "explorer" (after isCanvasMode at line 96)
       - Add callbacks using useCallback:
         handleEnterExplorer: calls enterExplorerMode() (per D-04, no dirty-state guard needed)
         handleExitExplorer: calls exitExplorerMode()
       - Add Cmd+E keyboard shortcut in the existing useEffect handler (lines 266-282): if (mod && e.key === "e") { e.preventDefault(); if (!isExplorerMode) { handleEnterExplorer(); } } (per D-12)
       - Update dependency array of the useEffect to include isExplorerMode and handleEnterExplorer
       - Update showHome conditional (line 357): add mode !== "explorer" (per RESEARCH.md Pitfall 1)
       - Update conditional render block (lines 384-429): add isExplorerMode branch between showHome and ReactFlowProvider:
         showHome ? <HomeScreen ... onEnterExplorer={handleEnterExplorer} /> : isExplorerMode ? <ExplorerShell onHome={handleExitExplorer} onOpenSettings={() => setSettingsOpen(true)} /> : <ReactFlowProvider>...</ReactFlowProvider>
       - Add import for ExplorerShell from @/features/explorer/components/explorer-shell
       - Pass onEnterExplorer={handleEnterExplorer} to HomeScreen

    Validation: npm run lint && npm run build
  </action>
  <verify>
    <automated>npm run lint && npm run build && npx vitest run --run</automated>
  </verify>
  <acceptance_criteria>
    - src/features/explorer/components/explorer-nav-bar.tsx exports ExplorerNavBar with onHome and onOpenSettings props
    - src/features/explorer/components/explorer-empty-state.tsx exports ExplorerEmptyState with onOpenSettings prop
    - src/features/explorer/components/explorer-shell.tsx exports ExplorerShell with onHome and onOpenSettings props
    - src/features/connection/components/home-screen.tsx contains FolderSync import and onEnterExplorer prop
    - src/features/connection/components/home-screen.tsx contains text "Integration Explorer" in a Button
    - src/App.tsx contains const isExplorerMode = mode === "explorer"
    - src/App.tsx contains mode !== "explorer" in the showHome conditional
    - src/App.tsx contains e.key === "e" in the keyboard handler
    - src/App.tsx contains ExplorerShell in the JSX render
    - npm run lint exits 0
    - npm run build exits 0 (includes TypeScript type checking)
    - npx vitest run --run exits 0 (all existing + new tests pass)
  </acceptance_criteria>
  <done>Three explorer components created following exact UI-SPEC contracts, home screen has Integration Explorer button with FolderSync icon between Canvas Mode and Settings, App.tsx renders ExplorerShell when mode is "explorer" and handles Cmd+E shortcut. Lint, build, and all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Explorer Shell</name>
  <what-built>Complete Explorer mode: home screen button, mode entry/exit, nav bar, and empty state content per UI-SPEC design contract</what-built>
  <how-to-verify>
    1. Run `npm run tauri dev` (or `npm run dev` for frontend-only)
    2. On the home screen, verify button order: Connect to Server, Canvas Mode, Integration Explorer, Settings, About
    3. Verify the Integration Explorer button has a FolderSync icon on the left and "Cmd+E" (or "Ctrl+E") badge on the right
    4. Click "Integration Explorer" -- verify the home screen is replaced by the Explorer shell
    5. Verify the nav bar: "Monocle" text (JetBrains Mono) on the left, Home button and Settings gear button on the right, both with tooltips on hover
    6. Verify the empty state is centered: FolderSync icon (muted), "Integration Explorer" heading, description text, and "Open Settings" button
    7. Click "Open Settings" -- verify the settings sheet opens
    8. Close settings, then click the Home button in the nav bar -- verify return to the home screen
    9. From the home screen, press Cmd+E (or Ctrl+E) -- verify it enters Explorer mode
    10. Verify theme: switch between light and dark mode in settings -- Explorer matches the existing app style
    11. If connected to a database before entering Explorer, verify that exiting Explorer and going back to the schema graph retains the connection
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None added | Phase 1 adds only static UI components. No data processing, no network requests, no file system access, no new Tauri commands. Attack surface is identical to the existing app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Mode state (Zustand store) | accept | Mode is a string union in client-side state. No server-side trust boundary. Single-user desktop app with no auth. |
| T-01-02 | Information Disclosure | Keyboard shortcut | accept | Cmd+E is a convenience shortcut with no data exposure. Standard browser keyboard event handling. |

No new packages installed. No package legitimacy audit required.
</threat_model>

<verification>
Phase-level verification after all tasks complete:

1. `npm run test -- --run` -- all tests pass (including 4 new store integration tests)
2. `npm run lint` -- no lint errors
3. `npm run build` -- TypeScript compiles without errors
4. Manual: Enter Explorer from home screen, see nav bar + empty state, return home via Home button
5. Manual: Cmd+E from home screen enters Explorer
6. Manual: Schema graph state preserved after Explorer round-trip
</verification>

<success_criteria>
- Integration Explorer button visible on home screen between Canvas Mode and Settings (per D-09)
- Clicking the button or pressing Cmd+E enters Explorer mode (per D-12)
- Explorer shows nav bar with Monocle branding, Home button, Settings button (per D-02)
- Empty state shows FolderSync icon, heading, description, and Open Settings CTA (per D-05)
- Home button returns to home screen instantly with no confirmation (per D-04)
- Settings button opens the existing AppSettingsSheet
- Mode switching does not affect Schema Graph state (per RESEARCH.md Pitfall 3)
- All 4 unit tests pass for store mode transitions
- npm run lint, npm run build, npm run test all pass
</success_criteria>

<output>
Create `.planning/phases/01-explorer-shell-navigation/01-01-SUMMARY.md` when done
</output>
