import { describe, it, expect } from "vitest";

describe("resolveSearchScope", () => {
  it("returns selected folder path for scope 'folder'", () => {
    expect(true).toBe(true);
  });

  it("returns source root path for scope 'source'", () => {
    expect(true).toBe(true);
  });

  it("returns all source paths for scope 'all'", () => {
    expect(true).toBe(true);
  });
});
